# Plano de Implementação: Gênesis Brain V2

## Visão Geral

Ordem por risco e dependência, seguindo as Fases 0–8 do `design.md`. Nenhuma tarefa de uma fase posterior deve começar antes do checkpoint da fase anterior, com exceção das tarefas explicitamente marcadas como paralelizáveis. Todo código novo segue a Fonte (`fonte-brain-v2.md`) — a seção citada em cada tarefa é a fonte de verdade; este arquivo não repete o código, aponta onde ele está.

Repositórios: **[API]** = `E:\Programas\wamp64\www\genesis-api` · **[FE]** = `c:\Users\felip\Downloads\G-nesis-2.0-main\G-nesis-2.0-main`.

**Lembrete de escopo (Fonte §89):** a ordem abaixo é de implementação interna. A promoção para produção é **uma única publicação** — nenhuma fase deste arquivo deve ser "lançada" isoladamente em produção; `GENESIS_BRAIN_V2_ENABLED` só liga em staging até o Release Gate (Fase 8) estar 100% aprovado.

Status de partida (auditoria de código real 2026-09-15, ver `design.md` → "Estado Atual Auditado"): a maior parte dos bugs P0 da Fonte está confirmada tal como descrita (score cast destrutivo, pesos 30/28/28/14, ausência de failover, `3h` fora de `INTERVALOS_BINANCE`, `strength` no caminho de targets, alavancagem no hash de idempotência, `MM_TIER1_FALLBACK`, `DesfechoService` parando no primeiro evento). Três pontos já parecem parcial ou totalmente resolvidos por specs anteriores (exclusão de `context` do bundle decisório, rastreamento de stop-fallback via `stop_motivo`, e possivelmente o live-merge do Plano B via `AnaliseTransformer`) — as tarefas correspondentes abaixo começam com investigação, não implementação direta.

## Tarefas

- [x] 1. Fase 0.1 — Medições obrigatórias antes de qualquer remoção
  - [x] 1.1 **[API]** Medir incidência de `stop_selection.candidate_id = null` nos logs atuais
    - Registrar por ativo, timeframe, provider/model, direção, período
    - **Resultado (2026-09-16, ver design.md → "Medição 1.1"):** medido contra a única base acessível deste sandbox (`genesisteste`, local, 145 análises). Só 10 linhas têm a chave `stop_selection` estruturalmente presente (campo é recente); nenhuma com `candidate_id=null` nessa amostra (0/10). Amostra pequena demais e não-produção — não conclui nada sobre a incidência real em produção, só que o mecanismo existe e está sendo exercitado. Repetir contra o banco de produção real antes de decidir qualquer coisa baseado nesse percentual.
    - _Requisitos: 12.1_
  - [x] 1.2 **[API]** Rodar benchmark 20x do bundle real congelado contra o provider fora do cache
    - **Substituída (2026-09-23):** o baseline do contrato legado perdeu o propósito — o benchmark V2 real (task 25.1, `genesis:benchmark-brain-v2`) foi executado e aceito pelo Felipe; não há mais o que comparar contra o contrato antigo, já removido.
    - Registrar amplitude de score, qualquer flip LONG/SHORT, provider/model/prompt/schema usados — este benchmark ainda usa o contrato **legado** (baseline); repetir com o contrato V2 na Fase 8
    - **Bloqueado (2026-09-16):** comando `genesis:benchmark-decision` já existe e roda o cenário certo (mesmo bundle congelado, 20x, sem cache — ver design.md). Tentativa real com imagem de análise verdadeira (id 168, BTCUSDT/4h) falhou na 1ª chamada: `OPENAI_RATE_LIMITED:429 insufficient_quota` — a conta OpenAI deste sandbox está sem crédito. Gemini não está acessível deste sandbox (achado de sessão anterior). Precisa rodar num ambiente com crédito OpenAI ou acesso a Gemini.
    - _Requisitos: 27.1_

- [x] 2. Fase 0.2 — Legacy Dependency Audit
  - [x] 2.1 **[API]** Preencher a tabela de classificação (`KEEP`/`REPLACE`/`REMOVE_FROM_DECISION_PATH`/`DELETE_AFTER_REPLACEMENT_VERIFIED`) para todo componente que toca `direction`/`score`/Plano A/Plano B/stop/target/RR
    - Mínimo: `ScoreFromFamilies`, `score_familias`, `score_breakdown` legado, pesos 30/28/28/14, `ScoreNarrativeBuilder`, `DirectionCoherenceGate`, `RegimeService`, `DerivativesReadingService`, `TargetCandidateCatalog::strength`
    - **Feito (2026-09-16):** tabela completa em design.md → "Legacy Dependency Audit (Fase 0.2)"
    - _Requisitos: 4.1_
  - [x] 2.2 Ler `DirectionCoherenceGate.php` e `RegimeService.php` por completo
    - Confirmar se escrevem em `direction`/`score`/`regime` hoje — determina se a Fase 1 remove escrita ou só confirma comportamento já correto
    - **Achado (2026-09-16):** nenhum dos dois escreve em `direction`/`score`/`regime`. Ambos são confirmados read-only/context-only — ver design.md para o detalhe. Task 7.1 não precisa remover escrita nenhuma, só documentar a confirmação (já feito).
    - _Requisitos: 6.1_

- [x] 3. Fase 0.3 — P0 independentes de contrato de decisão (paralelizável com Fase 1)
  - [x] 3.1 **[API]** Criar TTL por fonte em `config/binance.php`
    - Substituir `'cache_ttl' => env('BINANCE_CACHE_TTL', 300)` por array por fonte (`price`, `order_book`, `open_interest_current`, `funding_current`, `funding_history`, `exchange_info`, `leverage_brackets`)
    - **Feito (2026-09-16):** chaves nomeadas pelo vocabulário real do código (`price`/`candles`/`order_book`/`funding`/`funding_history`/`open_interest`/`open_interest_history`/`long_short_ratio`/`agg_trades`/`exchange_info`/`leverage_brackets`), não literalmente as da Fonte — reconciliado com `FreshnessPolicy::limits()`, que já definia idade máxima tolerável por fonte (ex.: `order_book` 60s) menor que o TTL antigo de 300s.
    - _Requisitos: 8.1_
  - [x] 3.2 **[API]** Persistir `source_observed_at`/`cache_stored_at`/`cache_age_seconds` em `BinanceService`
    - Nunca reescrever `observed_at` para `now()` só porque saiu do cache
    - **Feito (2026-09-16) + achado real:** `BinanceService::getWithFreshness()` novo guarda o envelope no cache; `get()` (compat) devolve só o payload. Achado ao investigar: `getOrderBookWalls()` SEMPRE devolvia `'timestamp' => now()`, mesmo em cache HIT — `DataFreshnessGate` nunca conseguia detectar livro de ofertas realmente velho porque o timestamp mentia. Corrigido para usar `source_observed_at` real. `market_price_observed_at` (`MarketSnapshotService::tempos()`) foi investigado e confirmado correto como está (fonte bypassa cache via `getCandlesStrict()`), não precisou de mudança.
    - _Requisitos: 8.2, 8.3_
  - [x] 3.3 **[API]** Criar `FailoverDecisionProvider.php`
    - Copiar estrutura da Fonte §66; `decision_provider_primary`/`decision_provider_fallback` configuráveis
    - **Feito (2026-09-16):** retry curto (250ms/500ms) no primário antes de falhar para o fallback; só falha de `TransportProviderException` aciona failover.
    - _Requisitos: 23.1_
  - [x] 3.4 **[API]** Classificar exceções de transporte vs. semânticas
    - Transporte (503/502/504/timeout/429/conexão) → retry curto + backoff + fallback; semântico (JSON inválido, score fora de enum, candidate_id inexistente) → repair, não consome orçamento de retry
    - **Feito (2026-09-16):** `TransportProviderException`/`SemanticProviderException` novas; `GeminiDecisionClient`/`OpenAiDecisionClient` reclassificados (mensagens de erro mantidas idênticas, só a classe mudou — testes existentes continuam passando). `AUTH_FAILED`/config ficam `\RuntimeException` genérica de propósito (nem retry nem repair resolvem).
    - _Requisitos: 23.2_
  - [x] 3.5 **[API]** Rebindar `DecisionProvider` em `GenesisGraphicalServiceProvider` para resolver o wrapper de failover
    - **Feito (2026-09-16):** binding monta primário+fallback e envolve com `FailoverDecisionProvider` antes do `LoggingDecisionProvider`. Nova config `decision_provider_fallback` (default `openai`).
    - _Requisitos: 23.4_
  - [x] 3.6 **[FE]** Retirar `alavancagem` do hash de `analysisIdempotency.ts`
    - `[symbol, timeframe, leverage, imageHash]` → `[symbol, timeframe, imageHash, canonicalMarketHash?]`
    - **Feito (2026-09-16):** `canonicalMarketHash?` não implementado (não existe hoje um sinal real pra passar aqui — ver achado da 3.7) — assinatura ficou `[symbol, timeframe, imageHash]`. Campo `alavancagem` removido do próprio tipo `SubmissaoAnalise` (não só da função), prova em nível de compilação de que a IA não pode mais reagir a alavancagem via este caminho.
    - _Requisitos: 17.1_
  - [x] 3.7 **[API]** Separar `analysis_identity_hash` de `execution_parameters_hash`
    - Alavancagem só entra no segundo
    - **Achado (2026-09-16), sem necessidade de código novo:** `AnalysisFingerprintService::make()` (o hash real de identidade hoje) nunca incluiu alavancagem — só o frontend (task 3.6) contaminava a identidade. `alavancagem_membro`/`margem_membro`/`liquidacao` já são gravados fora deste hash. Documentado no docblock da classe; nenhum campo/mecanismo novo criado (evita duplicar o que já existe, mesmo critério das 3 confirmações da Fase 0.2).
    - _Requisitos: 17.2_
  - [x] 3.8 **[API]** Criar `GenesisSupportedTimeframes::ALL = ['5m','15m','1h','4h','1d','1w']`
    - Substituir `allowed_timeframes` (16 valores) e reconciliar com `INTERVALOS_BINANCE` (15 valores, sem `3h`) — usar a nova classe como autoridade única em request validator, Binance, MTF, outcomes, frontend, expiração, telemetria
    - **Feito parcialmente (2026-09-16):** `GenesisSupportedTimeframes` criada; `config('genesis_graphical_v6.allowed_timeframes')` (request validator) já usa `::ALL` — fecha o bug real confirmado (`3h` passava na validação, reservava crédito, e só quebrava dentro do pipeline em `BinanceService::normalizarIntervalo()`). **Não estendido** a MTF/outcomes/telemetria/frontend nesta passada: `BinanceService::INTERVALOS_BINANCE` é fato da API (não produto, mantido); o frontend tem PELO MENOS 2 listas de timeframe divergentes entre si e do backend (`GenesisPage.tsx`, usadas em parsing de OCR/URL, não só num dropdown) — consolidar isso com segurança precisa de teste visual real, fora do que dá pra validar às cegas nesta sessão. Toda a suíte de testes (backend+frontend) só usa `4h`/`1d`/`1w`/`15m` — nenhum teste quebrou com a redução.
    - _Requisitos: 19.1, 19.2_
  - [x]* 3.9 Testes de timeframe e failover
    - `3h` rejeitado pré-fetch; cada um dos 6 timeframes canônicos funciona; Gemini 503 → retry → OpenAI fallback com mesmo bundle, sem refazer vision; erro semântico não consome retry de transporte
    - **Feito (2026-09-16):** `GenesisSupportedTimeframesTest` (3h rejeitado + 6 canônicos aceitos) e `FailoverDecisionProviderTest` (retry no primário, failover só em transporte, mesmo bundle/repair chega ao fallback, erro semântico nunca aciona failover) — 14 testes novos, todos verdes.
    - **Valida: Propriedade 5 (parcial), Requisito 19.4, 23.5**
    - _Requisitos: 19.3, 19.4, 23.5_

- [x] 4. Checkpoint Fase 0 — P0 corrigidos e medições registradas
  - Confirmar `rg "'3h'"` e `rg "MM_TIER1_FALLBACK"` ainda pendentes (tratados na Fase 6) vs. já corrigidos nesta fase
  - **Confirmado (2026-09-16):** `MM_TIER1_FALLBACK`/`MM_TIER1_FALLBACK_PADRAO` continuam 100% ativos em `LiquidationCalculatorService.php` — corretamente intocados (Fase 6, task 20.2). `'3h'` continua em `TimeframeNormalizer::CANONICOS` (mapeamento de rótulo de OCR, não um "aceito"), `ExecucaoService.php`/`MultiTimeframeSnapshotService.php`/`TargetCandidateCatalog.php` (tabelas de lookup agora inalcançáveis na prática, já que a validação de entrada não deixa mais `3h` passar — limpeza dessas tabelas mortas também é Fase 6, não feita agora de propósito). O único lugar onde `3h` realmente PASSAVA (request validator) está corrigido.
  - Perguntar ao usuário se há dúvidas antes de avançar para a Fase 1

- [x] 5. Fase 1.1 — Remover `ScoreFromFamilies` do caminho vivo
  - [x] 5.1 **[API]** Remover a chamada `$scoreFromFamilies->calcular((array) ($decision['score_familias'] ?? []))` de `GraphicalAnalysisAttemptJob.php:450`
    - Substituir por validação direta do score contra `GENESIS_V2_VALID_SCORES`
    - **Atenção:** fazer isto ANTES de mudar o schema (task 5.2) para não deixar o job dependente de um campo que o schema novo não exige mais
    - **Feito (2026-09-16):** chamada removida; job agora valida `$decision['score']` direto (lança `GENESIS_V2_INVALID_SCORE` se fora do enum) e não overwrita mais `score_description` com texto reconstruído por `ScoreNarrativeBuilder` — usa o texto que a própria IA escreveu (já validado por `DecisionResponseValidator`). `derivativesContextPublico()` (card público de derivativos, dependia de `score_familias.derivativos`) reescrito para derivar `strength` de onde a IA citou evidence_id de derivativos entre os drivers novos (6.1), não de família nenhuma.
    - _Requisitos: 5.1_
  - [x] 5.2 **[API]** Criar `GenesisDecisionSchema::GENESIS_V2_VALID_SCORES = [55,60,65,70,75,80,85,90]`
    - **Feito (2026-09-16).**
    - _Requisitos: 1.1_
  - [x]* 5.3 Rodar grep de confirmação
    - `rg -n "ScoreFromFamilies|score_familias|score_breakdown" app/ tests/` — confirmar que só restam ocorrências legadas/de comparação explícita
    - **Feito (2026-09-16):** zero ocorrência em código vivo — só comentários explicando a migração. `ScoreFromFamilies.php`/`ScoreNarrativeBuilder.php` continuam existindo (não deletados, `DELETE_AFTER_REPLACEMENT_VERIFIED` é Fase 8/task 25.2) mas sem nenhum consumidor no caminho vivo; seus próprios testes unitários (`ScoreFromFamiliesTest`/`ScoreNarrativeBuilderTest`) continuam verdes, testando a classe isolada.
    - _Requisitos: 5.5_

- [x] 6. Fase 1.2 — Contrato de decisão V2 completo
  - [x] 6.1 **[API]** Atualizar `GenesisDecisionSchema.php`
    - `direction` enum `LONG|SHORT`; `score` enum fechado; `regime`, `movement_character`, `primary_drivers`, `secondary_drivers`, `contrary_drivers`, `score_description`, `technical_analysis`, `stop_selection.candidate_id`, `target_ranking` (até 6 IDs), `plan_b` — copiar forma da Fonte §29-30
    - Remover `score_familias` do required V2
    - **Feito com um recorte deliberado (2026-09-16):** `VERSION` subiu para `decision-v7.0.0`. `score` (enum fechado, nullable como `direction`), `regime` (enum `TREND/RANGE/COMPRESSION/TRANSITION/REVERSAL_ATTEMPT`, vocabulário literal da Fonte §27), `movement_character` (string nullable, sem enum — a própria Fonte trata como "interpretação aberta da IA", um enum fechado rejeitaria estado legítimo) e `primary_drivers`/`secondary_drivers`/`contrary_drivers` (`{evidence_id, reason}`, validados contra `EvidenceCatalog`) — todos novos, todos implementados. **`target_selection`/`stop_selection`/`plan_b` NÃO foram renomeados/ampliados** (`target_ranking` de até 6 IDs continua sendo `target_selection`, cap de 3) — essa ampliação é o próprio corpo da task 18.1 (Fase 5, `TargetEligibilityService`), que consome os IDs extras; mudar o formato agora sem quem consuma o excedente criaria campo morto. Documentado no docblock da classe.
    - _Requisitos: 11.1_
  - [x] 6.2 **[API]** Atualizar `GenesisPrompt.php`
    - Remover pesos 30/28/28/14, votação por família, obrigação de `score_familias`, instrução de três alvos fixos
    - Adicionar: score discreto holístico, não contar sinais correlacionados (Fonte §32), derivativos podem participar da direção em conjunto (nunca isolados), MTF como contexto sem veto, figura/Fibo só se no bundle estruturado, dados ausentes simplesmente ausentes
    - **Feito (2026-09-16):** seção inteira "COMO DAR A FORÇA" (4 famílias + hierarquia de pesos) substituída por "COMO DAR O SCORE" (8 âncoras qualitativas, julgamento holístico) + seções novas de DRIVERS e REGIME/CARÁTER. **Achado real:** "instrução de três alvos fixos" não existia mais no prompt atual (já pedia "até 3, o menor entre 3 e o total disponível" — achado desta sessão, não código a mudar). "Figura/Fibo só se no bundle estruturado" e "dados ausentes simplesmente ausentes" também já existiam (regras A5/HIERARQUIA DE AUTORIDADE ponto 4) — não duplicado.
    - _Requisitos: 11.3_
  - [x] 6.3 **[API]** Atualizar `DecisionResponseValidator.php`
    - Remover validação das 4 famílias legadas; adicionar: `direction` válido, `score` no enum, `evidence_id` de cada driver existe em `EvidenceCatalog`, stop candidate existe, `target_ranking` sem duplicados e todos reais, visual citado existe em `bundle.vision`
    - **Feito (2026-09-16):** `SCORE_FAMILIA_INVALIDA` → `SCORE_INVALID`; `DRIVER_EVIDENCE_ID_INVALID` novo (contra `EvidenceCatalog::items()`); `REGIME_INVALID` novo. `stop_selection`/`target_selection` (ainda no formato atual, não `target_ranking`) já eram validados contra o catálogo real (`StopSelectionValidator`/`TargetSelectionValidator`, pré-existentes) — nada a duplicar aqui. Regra de `derivatives_summary` (podia ser null só com família INDISPONIVEL) reescrita para checar disponibilidade REAL no bundle em vez de opinião de família — achado real ao testar: `BenchmarkGenesisV69Test` tinha um fixture que só passava por declarar a família falsamente indisponível; a checagem nova pegou a inconsistência de verdade.
    - _Requisitos: 11.2_
  - [x] 6.4 **[API]** Remover casts destrutivos em `AnalysisPersistenceService.php:150,226`
    - Validar `is_int($score) && in_array(..., true)` antes de persistir; nunca `(int) $decision['score']`
    - **Feito (2026-09-16):** os DOIS casts confirmados e corrigidos (linha 151 alimentando `ExecutionPipelineService::generate()`, linha 226 no array de persistência) — `scoreValidadoOuFalha()` novo, único ponto de validação, lança `GENESIS_V2_INVALID_SCORE_AT_PERSISTENCE` em vez de coagir.
    - _Requisitos: 1.3, 1.4_
  - [x] 6.5 **[API]** Reduzir números livres do LLM
    - IA retorna `evidence_id`; backend formata o número já conhecido; reduz `UNACCOUNTED_NUMERIC_LITERAL`/`MONEY_FORMAT_RAW_NUMBER`/`NUMERIC_CITATION_LITERAL_NOT_FOUND`
    - **Não feito, adiado deliberadamente (2026-09-16):** isto é uma mudança de mecanismo maior que o resto da task 6 — hoje a IA escreve o número no texto livre e `numeric_citations` só AUDITA contra o valor real (`NUMERIC_CITATION_VALUE_MISMATCH`); o que a Fonte pede aqui é inverter o fluxo (IA cita só `evidence_id`, backend interpola o número formatado no texto), o que muda o formato de `numeric_citations`, o prompt e potencialmente o schema de novo. Optei por não empilhar essa mudança em cima de um contrato que já mudou de versão nesta mesma sessão (score/regime/drivers) sem o usuário revisar o corte atual primeiro — risco real de reduzir `MONEY_FORMAT_RAW_NUMBER`/`NUMERIC_CITATION_LITERAL_NOT_FOUND` mas quebrar validação já testada. Fica como item pendente explícito, não esquecido.
    - **Parcial (2026-09-23):** com os textos reais do benchmark (`genesis:benchmark-brain-v2`, report guarda a decisão completa), a maior parte das recusas era falso positivo do validador, não número livre da IA: "alta"→LTA, "ao longo"→LONG, "através"→ATR (substring cru), "15 minutos"/"50 e 200 períodos" como número não citado, Bollinger como elemento inventado. Corrigido (API `e3a74c6`): 8 decisões salvas revalidadas, 4 recusadas → 1, a restante é número real sem citação. A inversão de fluxo pedida aqui (IA cita `evidence_id`, backend formata o número) continua não feita.
    - **Fechado (2026-09-23), variante mais segura do mesmo objetivo (Fonte §82: "narrativa usa apenas números rastreáveis"):** em vez de mudar o contrato de novo (placeholders de `evidence_id` no texto), `DecisionMechanicalRepair` rastreia o número escrito até a evidência AVAILABLE do bundle (com `evidence_path` em valor composto) e acrescenta a citação; número sem fonte continua indo pro repair via IA. API `feat(brain-v2): auto-cite…`. Benchmark: 8/8 decisões limpas após o reparo.
    - _Requisitos: 11.4_
  - [x]* 6.6 Escrever testes de score e contrato
    - 55/60/90 aceitos; 0/50/73/95/100 rejeitados; `null` não vira zero; LONG/SHORT usam mesma escala; nenhum PHP altera `direction` após decisão validada
    - **Feito (2026-09-16):** `DecisionResponseValidatorTest` (score fora do enum rejeitado incluindo 0/50/73/95/100; cada um dos 8 valores aceito; driver com evidence_id inventado rejeitado; regime inválido rejeitado) + `AnalysisPersistenceServiceScoreV2Test` novo (score `null` lança em vez de persistir `0`; string numérica/fora do enum lançam; cada score válido persiste exatamente). "LONG/SHORT usam mesma escala" é estrutural (um `score` só, sem campo por direção — nada a testar em separado). "Nenhum PHP altera direction após decisão validada" fica para o `ScoreDirectionIsolationTest` da task 7.2 (Fase 1.3), que depende do achado da task 2.2 (já confirmado: `DirectionCoherenceGate` é read-only).
    - **Valida: Propriedade 1, Propriedade 2 (design.md)**
    - _Requisitos: 1.5, 2.1, 2.2_
  - **Achados da suíte completa pós-tasks 5/6 (2026-09-16), 4 correções reais fora do escopo original dos itens acima:**
    - **Regressão real:** a nova seção REGIME (item 6.2) usava "ainda sem confirmação" na descrição de `TRANSITION` — reintroduzia o radical CONFIRM banido pelo P0-02 (`GenesisPromptContractTest`, que varre o prompt inteiro, não só os campos de texto livre da IA). Corrigido para "ainda sem direção estabelecida".
    - `GenesisPromptScoreFamiliasTest` (contrato do prompt legado, 5 asserções) ainda exigia a presença do texto de família/hierarquia removido pelo item 6.2 — reescrito para trancar a AUSÊNCIA do modelo antigo e a PRESENÇA do novo (score 55-90, seção DRIVERS, seção REGIME).
    - `MANIFEST.json`/`VersionamentoSincronizadoTest`/`GenesisGraphicalV68ConfigTest`: `GenesisDecisionSchema::VERSION` subiu pra `decision-v7.0.0` (item 6.1) mas `config/genesis_graphical_v6.php` (`document_version`/`prompt_version`/`schema_version`) e o `MANIFEST.json` (deploy) não acompanharam — mesmo padrão de causa raiz F07 que esses testes existem pra travar. `document_version` sobe pra `'V2'`, `prompt_version` pra `'genesis-brain-v2-v7.0.0'`, `schema_version` pra `'decision-v7.0.0'`; manifesto regenerado (`deploy/gerar_manifesto.sh`).
    - **Achado de produto real, não só teste:** `config/genesis.php: conviccao_min_execucao` (piso de convicção mínima pra recomendar execução, `ExecucaoService`/`PlanRecommendationService`) estava em 50 — herdado da escala contínua 0-100 do modelo antigo. O score V2 é um enum de 8 valores com piso em 55, então com o config antigo TODO score V2 possível já nascia acima do mínimo e o reason_code `CONVICCAO_ABAIXO_MINIMO` virava código morto (nunca mais dispararia com dado real). A própria Fonte (§ config/genesis.php) já pedia essa atualização explicitamente: `'conviccao_min_execucao' => 60`. Subido para 60 em `config/genesis.php` E no `.env` local (`GENESIS_CONVICCAO_MIN_EXECUCAO`, que sobrescrevia o default do config) — **ação pendente do usuário: o `.env` de produção real também precisa desse mesmo ajuste (50→60) pro gate de convicção baixa continuar funcionando após o deploy do V2; não é algo que eu tenha acesso pra aplicar remotamente.** `conviccao_fraca_abaixo` (piso vizinho) fica em 50 — a Fonte só pede "revisar", sem valor novo obrigatório, e a chave não tem nenhum consumidor em código hoje (grep confirmado).

- [x] 7. Fase 1.3 — Rebaixar gates legados a telemetria (depende do achado da task 2.2)
  - [x] 7.1 **[API]** Ajustar `DirectionCoherenceGate.php`/`RegimeService.php` conforme achado da task 2.2
    - Se escrevem em `direction`/`score`/`regime`: remover escrita, manter só telemetria/auditoria
    - Se já são só leitura: nenhuma mudança de código, só documentar a confirmação
    - **Feito (2026-09-16), sem mudança de código:** reconfirmado por releitura completa dos dois arquivos, agora depois das tasks 5/6 (schema/prompt/validador reescritos) — a confirmação da task 2.2 continua verdadeira, nenhum drift. `DirectionCoherenceGate::contradicoes()` é leitura pura (bundle+direction→lista de descritores, nunca escreve); `validate()` só lê `score_basis.contradiction_level` (campo que sobreviveu à Fase 1.2 intacto, não fazia parte de `score_familias`) e devolve erros de repair, nunca reatribui `$decision['direction']`. `RegimeService::classificar()` não recebe nem devolve `direction` nenhuma — devolve `market.regime` como evidência `CONTEXT` (`EvidenceCatalog.php:84`, confirmado `decision_role=CONTEXT`, nunca `DECISION`), campo textualmente distinto do `regime` que a IA escreve no schema V2 (task 6.1) — documentado lado a lado no docblock de `GenesisDecisionSchema::regime`. Nenhum dos dois veta `direction` nem decide o `regime` final — Requisito 6.2/6.3 satisfeitos sem alteração de comportamento.
    - _Requisitos: 6.2, 6.3_
  - [x]* 7.2 Escrever `ScoreDirectionIsolationTest`
    - Para qualquer `family_scores`/drivers, nenhum modificador de contexto reverte `direction` já validada
    - **Valida: Propriedade 1 (design.md)**
    - **Feito (2026-09-16):** `tests/Unit/ScoreDirectionIsolationTest.php` novo, 26 casos (6 data providers) — `DerivativesDirectionIsolationTest` do spec irmão só existe hoje como saída capturada em `tests/Proof/_archive_v4_3_r3_2` (o mecanismo que testava, modificador de derivativos por família, não existe mais no V2), então este teste foi desenhado do zero seguindo o mesmo espírito, adaptado a `drivers`. Cobre: (1) `DirectionCoherenceGate::contradicoes()` com as 5 famílias de contradição objetiva disparando ao mesmo tempo contra LONG e contra SHORT — nenhum item devolvido pode carregar uma chave `direction`/`direcao`; (2) `validate()` no mesmo cenário pior-caso preserva `$decision['direction']` exatamente; (3) `RegimeService::classificar()` em 6 combinações de indicadores nunca devolve chave `direction`, `direcao_estrutura` nunca usa o vocabulário `LONG`/`SHORT`, e `regra` é sempre `CONTEXTO_NAO_DECISORIO`; (4) ponta a ponta via `AnalysisPersistenceService::persistIntoExisting()` — `direcao` persistida bate exatamente com a decisão pra LONG/SHORT × sem drivers/com 4 `contrary_drivers` (teto do schema). Não duplica `DirectionCoherenceGateTest` (já cobre cada família de contradição individualmente) — foca especificamente na propriedade de isolamento, não na detecção.
    - _Requisitos: 2.3_

- [x] 8. Checkpoint Fase 1 — Score V2 vivo, legado fora do job de decisão
  - Rodar suíte de testes de score + `rg` de confirmação da task 5.3
  - Perguntar ao usuário se há dúvidas antes de avançar para a Fase 2
  - **Feito (2026-09-16):** suíte completa do backend rodada 3x nesta sessão (após tasks 3/4, após tasks 5/6, e após a rodada de correções acima) — 1072 passed/13 skipped/0 failed na versão final, contra as 11 falhas reais encontradas e corrigidas (ver notas da task 6 acima). `rg` da task 5.3 confirmado limpo.
  - **Flake pré-existente, não causado por esta sessão, não investigado a fundo (fora de escopo das tasks 5/6/7):** `GraphicalAnalysisAttemptJobTest::falha_apos_esgotar_as_3_tentativas` e `GraphicalAnalysisFullPipelineIntegrationTest` ocasionalmente terminam em `PENDING` (esperado `FAILED`/`COMPLETED`). Hipótese mais provável: os dois dependem de `Artisan::call('queue:work', ['--stop-when-empty' => true])` processando múltiplas tentativas com backoff entre elas (retry do job + `FailoverDecisionProvider`); `--stop-when-empty` decide "fila vazia" pela ausência de jobs IMEDIATAMENTE disponíveis, então um job acessível só depois do backoff pode ser lido como "fila vazia" e o worker para cedo — uma corrida de tempo real. **Correção após a task 7 (2026-09-16):** não é exclusivo da suíte completa como a nota original desta sessão registrou — reproduziu 1x também num lote pequeno de 5 arquivos (`ScoreDirectionIsolationTest` + `GraphicalAnalysisAttemptJobTest` juntos), e a mesma combinação passou limpa 4x seguidas seções depois. É uma corrida de tempo sensível a carga/timing do momento, não a quantidade de testes na mesma execução — mais provável sob carga (daí aparecer mais na suíte completa de ~500s), mas não determinística por tamanho de lote. Não é uma regressão desta sessão nem de nenhum arquivo novo criado nela (script idêntico já falhava assim depois das tasks 3/4, antes de qualquer código das tasks 5/6/7 existir). Requer trabalho de infraestrutura de fila (ex.: trocar `--stop-when-empty` por um loop com deadline, ou mockar o tempo do backoff nesses 2 testes) — decisão de produto/engenharia que o usuário deveria revisar antes, não algo pra eu resolver unilateralmente dentro do escopo desta task. **Confirmação após a task 12 (2026-09-17):** o novo `test_stop_selection_invalida_persistente_degrada_para_fallback_automatico_em_vez_de_falhar` (mesmo mecanismo `--stop-when-empty`, 2 tentativas reais) pegou o mesmo flake 1x rodando junto com `GraphicalAnalysisFullPipelineIntegrationTest` na suíte completa — reproduzido de propósito depois: rodou sozinho (10/10 limpo) e em par com `GraphicalAnalysisFullPipelineIntegrationTest` mais uma vez (11/11 limpo) sem tocar em nada. Mais uma confirmação de que é timing/carga do momento, não um arquivo específico ou uma regressão de código.

- [x] 9. Fase 2 — Features preditivas (paralelizável com Fase 1, integração depende dela)
  - [x] 9.1 **[API]** Criar `PredictiveFeatureService.php`
    - Serviço puro (sem HTTP, sem julgamento LONG/SHORT); `ema_dynamics`, `dmi_dynamics`, `macd_dynamics`, `atr_dynamics` per Fonte §17; estender com `cvd_dynamics`/`oi_dynamics`/`funding_dynamics` per Fonte §16.11-16.13
    - **Feito (2026-09-17):** classe pura criada (`app/Services/GraphicalAnalysis/PredictiveFeatureService.php`) — recebe séries JÁ COLETADAS (nunca calcula indicador do zero, nunca faz I/O) e devolve slope/aceleração/distância em ATR/compressão. `TechnicalAnalysisService` ganhou 3 métodos públicos novos (`adxSeries()`/`macdSeries()`/`atrSeries()`, mesma matemática de Wilder/EMA que os métodos privados já usavam, verificados byte-a-byte contra `adx()`/`macd()`/`atr()` existentes antes de qualquer uso). Escopo deliberado pra não duplicar leitura que já existe: `cvdDynamics()` não repete `cvd.divergence`; `oiDynamics()` não repete `open_interest_context`/`derivatives.reading`; `fundingDynamics()` não repete média/desvio/z-score (`MarketSnapshotService::fundingStats()`).
    - _Requisitos: 7.1_
  - [x] 9.2 **[API]** Adicionar IDs ao `EvidenceCatalog.php`
    - `trend.ema_dynamics`, `momentum.dmi_dynamics`, `momentum.macd_dynamics`, `volatility.atr_dynamics`, `flow.cvd_dynamics`, `flow.trade_flow`, `derivatives.oi_dynamics`, `derivatives.funding_dynamics`, `multi_timeframe.context`
    - **Feito (2026-09-17):** 8 ids novos adicionados (papel DECISION espelhando o indicador "pai" de cada um; `volatility.atr_dynamics` fica CONTEXT, mesmo papel de `volatility.atr14`/`atr_percent`). **`multi_timeframe.context` não é um id novo** — já existe desde antes como `timeframes.context` (path `multi_timeframe`); tratado como já satisfeito, sem duplicar.
    - _Requisitos: 7.3_
  - [x] 9.3 **[API]** Integrar `PredictiveFeatureService` ao `CanonicalBundleBuilder` sob os 4 grupos (FACTS/DERIVED_FEATURES/VISION/AVAILABILITY)
    - Só conectar depois que o contrato V2 (Fase 1) já aceita os novos `evidence_id`
    - **Feito com um recorte deliberado (2026-09-17):** conectado em `MarketSnapshotService::collect()` (que já monta o `$snapshot` que `CanonicalBundleBuilder` consome, nenhuma mudança extra precisou entrar em `CanonicalBundleBuilder.php` — `EvidenceManifestBuilder` já escaneia o snapshot pelo `path` do catálogo). ema/dmi/macd/atr dynamics vêm de `TechnicalAnalysisService::*Series()` sobre os mesmos candles fechados que já alimentam `calcular()`; `cvd.dynamics` reaproveita `$cvdSeries` já coletado; `derivatives.oi_dynamics`/`funding_dynamics` reaproveitam `$oiHistory`/`$fundingHistory` já buscados. **`flow.trade_flow` é uma mudança de arquitetura real, não só um wiring**: antes era `DISPLAY_ONLY`, exclusivo de `DerivativesTradeFlowController`, com um docblock explícito dizendo "nunca entra no bundle do cérebro" — a Fonte pede o oposto agora, e o item foi promovido a `DECISION` no bundle decisório de verdade, usando a mesma `FeaturePolicy::forTimeframe()` que já limitava isso a timeframes intraday. **Os 4 grupos conceituais (FACTS/DERIVED_FEATURES/VISION/AVAILABILITY) do Requisito 7.2 não foram retrofitados em todo o catálogo existente** (~64 itens) — isso é uma categorização nova, ortogonal ao `role` (DECISION/CONTEXT/DISPLAY_ONLY) que já existe, e o Requisito 7.2 fala em "organizar TODO campo do bundle", um escopo bem maior que esta task específica pede. Documentado aqui e no comentário do catálogo qual grupo cada campo NOVO pertenceria (ema/dmi/macd/atr/cvd/oi/funding dynamics = DERIVED_FEATURES; trade_flow = FACTS+metadado), sem inventar uma migração de taxonomia retroativa sem pedido explícito.
    - Verificado ponta a ponta com dado real da Binance (`tests/Feature/PredictiveFeaturesWiringTest.php`) — todos os itens chegam `AVAILABLE` em `1h` e `1d`.
    - _Requisitos: 7.2_
  - [x] 9.4 **[API]** Estender `TradeFlowService` com metadados de cobertura
    - `coverage_start_ms`/`coverage_end_ms`/`requested_window_seconds`/`truncated`; 15m usa trade flow completo, 1h com janelas controladas, 1d/1w `NOT_APPLICABLE`
    - **Feito (2026-09-17):** `resumir()` ganhou um 4º parâmetro opcional `$fetchStartMs` (retrocompatível — `DerivativesTradeFlowController` atualizado pra passar o que já calculava) e 4 campos novos por janela. `truncated` distingue duas causas pro mesmo sintoma (poucos/zero trades): coleta que não alcançou o início teórico da janela (`truncated=true`) vs. mercado genuinamente quieto (`truncated=false`, dado alcança o início, só é esparso) — a política de timeframe (`FeaturePolicy::forTimeframe()`) já existia e já cobria exatamente a granularidade que a Fonte pede (intraday 1m-1h habilitado, resto desabilitado); não precisou de mudança, só reconfirmação.
    - _Requisitos: 7.5_
  - [x] 9.5 **[FE]** Confirmar que nenhum card novo de feature preditiva foi adicionado à UI
    - **Feito (2026-09-17):** nenhuma mudança de frontend nesta task; grep confirmou zero referência aos novos nomes de campo (`ema_dynamics`/`dmi_dynamics`/etc.) fora dos próprios documentos de spec.
    - _Requisitos: 7.4_
  - [x]* 9.6 Testes de `PredictiveFeatureService` isolado
    - Sem rede; slope/aceleração/compressão calculados corretamente sobre séries fixas; trade flow trunca sem fingir cobertura completa
    - **Feito (2026-09-17):** `PredictiveFeatureServiceTest` (16 testes, séries lineares com aceleração zero calculada à mão + séries com curvatura real, normalização por ATR, degradação pra `null` sem fabricar valor) + `TradeFlowServiceCoverageTest` (4 testes, distingue truncamento de coleta vs. mercado quieto) + `EvidenceCatalogPredictiveFeaturesTest` (9 testes, path/role de cada id novo) + `PredictiveFeaturesWiringTest` (4 testes, ponta a ponta com Binance real).
    - _Requisitos: 7.1, 7.5_

- [x] 10. Fase 3 — Vision anti-alucinação e multi-timeframe único
  - [x] 10.1 **[API]** Confirmar comportamento atual de `MultiTimeframeSnapshotService.php`
    - Ler o arquivo completo; confirmar se hoje consulta 1 ou 2 HTF, e se algum vira veto
    - **Achado (2026-09-17):** lido por completo. Consultava sempre DOIS HTF por análise (mapa antigo de 16 entradas, ex.: `15m → ['1h','4h']`) — confirma a suspeita do Requisito 10. Nenhum dos dois HTF vira veto em lugar nenhum: `collect()` só devolve `{timeframe,status,price,ema21,ema50,rsi,macd,bias}`, consumido por `DirectionCoherenceGate::contradicoes()` (`timeframes.context`) só como descritor de contradição ADVISÓRIO (nunca reatribui `direction`, confirmado de novo na task 7) e pelo prompt (`GenesisPrompt.php`, já reescrito na task 6.2) que já instrui explicitamente "entra como CONTEXTO, nunca como veto". Achado extra: o método usava a série de candles BRUTA (com a vela do HTF em formação incluída) pros indicadores — o gráfico principal já tinha revertido isso pra candles fechados (V6.9 correção técnica, item 19), este método tinha ficado pra trás.
    - _Requisitos: 10.1_
  - [x] 10.2 **[API]** Ajustar para mapa de um único HTF (`5m→15m`, `15m→1h`, `1h→4h`, `4h→1d`, `1d→1w`, `1w→nenhum`)
    - Remover consulta automática a dois HTF se confirmada na task 10.1; garantir candles fechados para indicadores de contexto
    - **Feito (2026-09-17):** mapa antigo (16 chaves × até 2 HTF) substituído pelo mapa de exatamente 6 entradas × 1 HTF da Fonte §26 (as demais 10 chaves antigas mapeavam timeframes que `GenesisSupportedTimeframes::ALL` já não aceita mais desde a task 3.8 — nunca mais alcançáveis por uma requisição real). Candles agora filtrados pelo mesmo `candlesFechados()` que o pipeline principal usa, antes de calcular indicadores do HTF.
    - _Requisitos: 10.1_
  - [x] 10.3 **[API]** Garantir que HTF nunca veta a direção do timeframe atual
    - **Já satisfeito (2026-09-17), nenhuma mudança de código:** confirmado pela task 10.1 (nunca foi um veto) e pelas tasks 6.2/7 (prompt já instrui isso, `DirectionCoherenceGate` já é read-only). Travado em teste nesta task (`MultiTimeframeNoVetoTest`).
    - _Requisitos: 10.2_
  - [x]* 10.4 Testes de vision e MTF
    - `patterns:[]` → narrativa não cita figura; Fibo sem 2 âncoras válidas não participa; LTA com 1 ponto descartada; LONG tático sobrevive a HTF bearish
    - **Feito com achado real (2026-09-17):** os 3 primeiros itens (vision anti-alucinação, Requisito 9 inteiro) já estavam implementados E testados exaustivamente desde a V6.9 (`GeminiVisionServiceTest`: `test_pattern_fora_do_catalogo_e_descartado_e_logado`, `test_a5_lta_com_dois_pontos_ancorados_e_preservada`/`test_a5_lta_sem_ponto_fim_e_descartada_e_logada`, `test_fibonacci_sem_visible_price_e_descartado`/`test_fibonacci_sem_label_e_descartado`/`test_fibonacci_com_confianca_abaixo_do_piso_e_descartado`; mais `NarrativeFidelityGateA5Test`/`NarrativeFidelityGateTest` do lado da narrativa) — nada novo a escrever aí, só confirmado por leitura antes de declarar a task feita. Item novo de verdade: `MultiTimeframeNoVetoTest` (2 testes) — reproduz o exemplo exato da Fonte (1h BEARISH + LONG em 15m = `COUNTERTREND_REBOUND`, não contradição a esconder): `DirectionCoherenceGate` reconhece a contradição (auditável) mas nunca força repair nem reverte `direction` quando reconhecida; `GenesisPrompt::system()` contém a instrução explícita e o rótulo de exemplo. Também atualizado `MultiTimeframeConfluenceTest` (esperava o comportamento de 2 HTF removido na task 10.2) com 2 testes novos provando o mapa de 1 HTF.
    - **Valida: Propriedade 4 (design.md)**
    - _Requisitos: 9.1-9.6, 10.3_

- [x] 11. Checkpoint Fase 3 — Contrato de decisão e vision completos
  - Rodar suíte de testes de vision/MTF + testes de score da Fase 1 juntos (regressão)
  - Perguntar ao usuário se há dúvidas antes de avançar para a Fase 4
  - **Feito (2026-09-17):** 186/186 testes verdes (`PredictiveFeatureServiceTest`, `TradeFlowServiceCoverageTest`, `EvidenceCatalogPredictiveFeaturesTest`, `PredictiveFeaturesWiringTest`, `MultiTimeframeConfluenceTest`, `MultiTimeframeNoVetoTest`, `DirectionCoherenceGateTest`, `ScoreDirectionIsolationTest`, `DecisionResponseValidatorTest`, `AnalysisPersistenceServiceScoreV2Test`, `GenesisPromptScoreFamiliasTest`, `GenesisPromptContractTest`, `VersionamentoSincronizadoTest`, `EvidenceManifestBuilderH47Test`, `GeminiVisionServiceTest`, `NarrativeFidelityGateA5Test`). Usuário já tinha direcionado explicitamente "execute fase 11, 12 e 13" na mesma instrução — não houve pausa pra perguntar antes de avançar, autorização já dada.

- [x] 12. Fase 4.1 — Reconciliar stop fallback existente com `stop_source`
  - [x] 12.1 **[API]** Ler `NivelService.php`/`StopSelectionValidator.php` completos
    - Confirmar exatamente como `stop_motivo`/`MENSAGEM_STOP_FALLBACK_ESTRUTURAL`/`$ancoraEscolhidaPelaIa` funcionam hoje antes de desenhar `stop_source`
    - **Feito (2026-09-17):** os dois arquivos lidos por completo (563 + 59 linhas). Confirmado: `$ancoraEscolhidaPelaIa` (booleano local de `NivelService::stop()`) já é a ÚNICA fonte de verdade que distingue "a IA escolheu esta âncora" de "o sistema automático de 3 camadas assumiu" — já decide `stop_motivo` hoje (V6.9 item 34), só nunca tinha sido exposta com o vocabulário `stop_source`. `StopSelectionValidator::validate()` já trata `candidate_id === null` como resposta válida (ponto 7) e cai graciosamente no automático quando o id é desconhecido/lado errado/fora do teto — nunca rejeita o stop inteiro por uma escolha ruim da IA.
    - _Requisitos: 12.2_
  - [x] 12.2 **[API]** Adicionar/reconciliar `stop_recommended`, `stop_effective`, `stop_source` (`AI_RECOMMENDED|USER_ADJUSTED|SYSTEM_FALLBACK`)
    - Mapear o mecanismo existente em vez de duplicá-lo
    - **Feito (2026-09-17):** `stop_source` adicionado bem ao lado de `stop_motivo` em `NivelService::stop()`/`stopIndisponivel()`, usando a MESMA variável `$ancoraEscolhidaPelaIa` — zero lógica nova de decisão. `stop_recommended` é alias de `nivel`/`stop`. Propagado por `ExecucaoService::montar()` (Plano A e Plano B) e `PlanoBService::gerar()`. `stop_effective` nasce sempre igual a `stop_recommended` (nenhum ajuste ainda acontece no backend — o endpoint de repricing que persistiria uma divergência real é a Fase 4.3/task 14, fora do escopo desta sessão). **Escopo deliberado, sem migration:** os 3 campos novos NÃO foram adicionados como colunas em `genesis_analise_planos` (`AnalisePlano::$fillable`) — essa tabela alimenta `AnaliseTransformer` (histórico/outcomes), um caminho SEPARADO do que a tela de resultado de UMA análise usa (`AnalysisPublicResponseBuilder` lê `pipeline.execution`, o array inteiro de `ExecucaoService::montar()` já serializado como evidência — confirmado por leitura direta, não suposição). Os 3 campos já chegam corretamente pela rota que interessa pra esta fase (a tela de resultado / o slider da task 13); adicionar colunas exigiria uma migration, que esta sessão não roda sem autorização explícita do usuário (regra já estabelecida) — e a Fonte não define esse segundo caminho como parte do escopo desta task. Documentado aqui para revisão futura, não esquecido.
    - _Requisitos: 12.2_
  - [x] 12.3 **[API]** Aplicar regra de fallback em cascata
    - Repair semântico único → fallback V2 auditado (`stop_source=SYSTEM_FALLBACK`, nunca escondido) → `execution_state=BLOCKED_NO_VALID_STOP` sem inventar preço
    - **Feito (2026-09-17), achado real:** "repair semântico" já era o comportamento padrão (qualquer erro `STOP_SELECTION_*` já força uma nova chamada à IA, mesmo bundle) — nada a mudar aí. Gap real encontrado: se a IA insistisse numa escolha de stop inválida em TODAS as tentativas, a análise inteira falhava (`MODEL_OUTPUT_INVALID_AFTER_REPAIR`) mesmo quando `NivelService::stop()` já tinha um fallback automático perfeitamente capaz de produzir um stop válido — nunca chegava a usá-lo, porque o validador rejeitava a decisão inteira antes. Corrigido em `GraphicalAnalysisAttemptJob::handle()`: a partir da 2ª tentativa (repair já tentado pelo menos 1x), se os ÚNICOS erros restantes forem `STOP_SELECTION_*` (`StopSelectionValidator::apenasErrosDeSelecao()`, novo), o job descarta só a escolha inválida (`candidate_id=null`) e revalida — só aceita se o resto do contrato passar limpo. **Decisão deliberada de manter fora de `DecisionMechanicalRepair`**: aquela classe documenta explicitamente (14/09/2026, mesma sessão de investigação) que seleção de stop/alvo fica FORA do escopo de repair mecânico — mas "descartar uma escolha inválida em favor do fallback determinístico que já existe" é conceitualmente diferente de "inventar uma escolha válida em nome da IA" (o que aquela exclusão proíbe); implementado como lógica separada no job, não uma violação silenciosa daquela decisão. `execution_state=BLOCKED_NO_VALID_STOP`: **já existe sob outro nome** — `stop_status=STOP_UNAVAILABLE` (V6.7, doutrina DP-03/A-14: análise imperfeita continua existindo/executável, nenhum preço é fabricado) já é exatamente esse estado; não renomeado (mudança grande, só cosmética, tocaria dezenas de arquivos/testes já maduros) — reconciliado por documentação, não por rename.
    - _Requisitos: 12.3, 12.4_
  - [x] 12.4 **[FE]** Corrigir `Number(null)===0` no frontend
    - Criar `hasValidNumber()` explícito; validar lado (`LONG: stop<entrada`, `SHORT: stop>entrada`)
    - **Feito com um recorte investigado (2026-09-17):** único uso de `Number.isFinite(Number(x))` sobre stop no frontend é `AnalysisResult.tsx` (`planoAtivoCompleto`) — lido com atenção e confirmado DELIBERADO (comentário próprio já documenta "DP-03": stop null não pode derrubar a tela de execução, só `undefined` reprova). Não alterado — mudar isso reverteria uma correção anterior válida. `hasValidNumber()`/`isStopSideValid()`/`isStopPositionValid()` criados do zero em `utils/stopValidation.ts` (10 testes) — infraestrutura pronta e já em uso real pelo slider da task 13, que É o lugar onde a validação estrita de fato precisa acontecer (posição do slider, não "a tela deve renderizar").
    - _Requisitos: 12.5_

- [x] 13. Fase 4.2 — Slider e régua de risco bidirecional
  - [x] 13.1 **[FE]** Implementar slider de stop com posição inicial `stop_recommended`
    - Marcar visualmente "STOP GÊNESIS"
    - **Feito (2026-09-17):** `components/StopSlider.tsx` novo — `<input type="range">` controlado, estado local (`useState`) inicializado em `stopRecommended`, marcador fixo "STOP GÊNESIS" posicionado por % ao longo do range. Integrado em `AnalysisResult.tsx` na seção "Stop de proteção" existente, só quando `stop_status !== STOP_UNAVAILABLE`.
    - _Requisitos: 12.6_
  - [x] 13.2 **[FE]** Implementar régua `TOO_CLOSE_NOISE → CAUTION_CLOSE → TECHNICAL_ZONE → CAUTION_WIDE → TOO_WIDE_LIQUIDATION_RISK`
    - Lado curto usa `stop_distance_atr` (config `noise_red_below_atr`/`noise_yellow_below_atr`); lado distante usa distância real até liquidação — nunca manutenção estimada
    - **Feito (2026-09-17):** `utils/stopRiskZone.ts` (13 testes) — `classifyStopRiskZone()` pura, lado curto por ATR (limiares exatos do exemplo da Fonte, 0.50/0.80), lado distante por `liqGapPctOfEntryToLiquidation()` (% real da distância entrada→liquidação que ainda resta além do stop — nunca `null` vira zona colorida). Limiares espelhados em `config('genesis.stop_slider')` (genesis-api, novo) para quando o endpoint de repricing (Fase 4.3) existir e precisar da mesma classificação no backend — documentado que os dois precisam ficar sincronizados manualmente até lá.
    - _Requisitos: 12.7_
  - [x] 13.3 **[FE]** Bloquear posições matematicamente inválidas do slider
    - `LONG: liquidation<stop<entry`; `SHORT: entry<stop<liquidation`; se liquidação `UNAVAILABLE`, permitir RR/risco mas não fingir zona verde/vermelha
    - **Feito (2026-09-17):** `isStopPositionValid()`/`isStopSideValid()` (utils/stopValidation.ts) usados pelo próprio range (`min`/`max` do `<input type="range">` já excluem posições inválidas por construção — nunca alcançáveis pelo drag, não só bloqueadas depois) mais um aviso explícito quando o lado está errado. Liquidação `UNAVAILABLE`: `min`/`max` caem num teto sintético (6 ATR) em vez do preço de liquidação real, slider continua funcional pra ruído/zona técnica, nunca finge vermelho/verde de liquidação (testado: `liqGapPctOfEntryToLiquidation` devolve `null`, `classifyStopRiskZone` nunca entra nos ramos de liquidação com `null`).
    - _Requisitos: 12.8_
  - [x] 13.4 **[API/FE]** Garantir que mover o stop não chama Brain/IA nem re-executa análise técnica
    - **Feito (2026-09-17):** `StopSlider.tsx` não importa `services/api`/`services/geminiService`, não referencia `fetch`/`axios`/`XMLHttpRequest`, não usa `useEffect` (só `useState` local) — estruturalmente incapaz de fazer I/O, travado em teste por leitura de fonte (`stopSliderNoNetworkCall.test.ts`, mesmo padrão de checagem por fonte já usado neste projeto pra propriedades sem harness de render). `direction`/`score` nunca chegam a este componente (só recebe `direcao: 'LONG'|'SHORT'` já resolvida, nunca toca o schema de decisão).
    - _Requisitos: 12.9_

- [x] 14. Fase 4.3 — Endpoint de repricing
  - [x] 14.1 **[API]** Criar `POST /api/v1/analises/{uuid}/reprice`
    - Request/response per Fonte §39; matemática pura, nenhuma chamada a `DecisionProvider`
    - **Feito (2026-09-17):** `RepricingService.php` novo — lê `entrada`/TPs/`custos_bps` do plano JÁ ESCOLHIDO (congelado em `evidence_manifest['pipeline.execution']`, nunca recandidata alvos/stop do zero) e ATR do MESMO evidence_manifest (nunca re-executa `TechnicalAnalysisService`). Reusa `ExecucaoService::calcularRrPorAlvo()`/`calcularTamanhoSugerido()` (visibilidade ampliada de private pra public, zero duplicação de matemática já testada) e `LiquidationCalculatorService`. `StopRiskZoneClassifier.php` novo — porta PHP de `utils/stopRiskZone.ts` (task 13.2), mesmos limiares via `config('genesis.stop_slider')`. Rota em `routes/genesis_graphical_v6.php` (mesmo grupo/middleware `genesis.auth` de `/analises/{id}`), controller `AnaliseController::reprice()` reusando `resolveAnalise()` (mesmo escopo por dono, A01-A03). **Escopo deliberado, sem persistência:** a resposta é preview puro — `stop_effective`/`stop_source=USER_ADJUSTED` não são gravados em `genesis_analise_planos` (mesma trava de migration documentada na task 12.2); confirmado por teste (`test_reprice_nunca_persiste_nada_na_analise_original`).
    - _Requisitos: 13.1, 13.2_
  - [x] 14.2 **[FE]** Implementar preview local em `onChange` com a mesma fórmula pura do backend
    - Chamar `/reprice` em `onChangeEnd`/debounce curto; backend é fonte final
    - **Feito (2026-09-17):** `StopSlider.tsx` ganhou debounce de 450ms (`useEffect`+`setTimeout`, cancela o anterior a cada novo valor) chamando `reprecificar()` (novo, `services/api.ts`, mesmo padrão de `selecionarZona()`) só quando `analysisUuid`/`plan`/`leverage`/`equity` estão disponíveis — sem eles, o componente continua funcionando só com o preview local puro (Fase 4.2 original). Resposta do backend vira fonte final de RR/risco/liquidação exibidos quando corresponde à posição atual do slider (`repriceConfere`); enquanto isso, ou sem `analysisUuid`, mostra o preview local. `AnalysisResult.tsx` passa `alavancagem`/`capital_base_usd` do próprio plano (não há form de leverage/equity editável nesta tela ainda — o membro está ajustando o STOP, não o resto).
    - _Requisitos: 13.3_
  - [x] 14.3 **[API]** Recalcular apenas o que depende matematicamente do stop
    - RR1/RR2/RR3, distância entrada-stop, risco% sempre; quantidade/nocional/margem só quando sizing é baseado em risco fixo
    - **Já satisfeito por construção (2026-09-17):** `calcularRrPorAlvo()`/distância/risco% sempre rodam; `calcularTamanhoSugerido()` (reusada, não duplicada) já degrada sozinha pra `null` quando `risco_por_analise` não está configurado — mesmo comportamento do plano original, nada novo a implementar.
    - _Requisitos: 13.4_
  - [x]* 14.4 Testes de stop e repricing
    - Slider nunca chama IA; mover stop recalcula RR; `stop_recommended` permanece salvo; `stop_user_adjusted` fica separado; LONG/SHORT rejeitam lado errado
    - **Feito (2026-09-17):** `StopRiskZoneClassifierTest` (13 testes, espelha `stopRiskZone.test.ts`) + `RepricingServiceNeverCallsDecisionProviderTest` (prova por fonte, sem comentário) + `RepricingEndpointTest` (9 testes: RR recalcula com novo stop, `stop_recommended` nunca muda entre 2 chamadas com stops diferentes, `stop_source=USER_ADJUSTED` quando diverge/preserva o original quando não, LONG e SHORT rejeitam lado errado, 404 por dono, nunca persiste) + `stopSliderNoNetworkCall.test.ts` reescrito (6 testes: só `reprecificar()` é chamado, sempre dentro do debounce, nunca no handler direto do range, nunca `geminiService`).
    - **Valida: Propriedade 6 (design.md)**
    - _Requisitos: 12.5, 13.4_

- [x] 15. Checkpoint Fase 4 — Stop V2 e repricing funcionando sem nova IA
  - Testar manualmente: mover slider, confirmar RR/margem/liquidação atualizam sem chamada à IA (verificar logs/rede)
  - Perguntar ao usuário se há dúvidas antes de avançar para a Fase 5
  - **Feito com uma ressalva honesta (2026-09-17):** sem clique real em navegador (sessão autônoma, sem ambiente de browser interativo disponível) — a prova é automatizada, não manual: `stopSliderNoNetworkCall.test.ts`/`RepricingServiceNeverCallsDecisionProviderTest` provam por leitura de fonte que nenhum caminho do slider/reprice toca `DecisionProvider`/`geminiService`; `RepricingEndpointTest` prova RR/risco/liquidação recalculando de verdade contra um plano real persistido. Suíte completa rodada (backend + frontend) sem regressão. Recomendo um clique manual real antes de considerar isto pronto pra produção — não fiz essa alegação.
  - Usuário já tinha direcionado "execute 14,15 e 16" na mesma instrução — não houve pausa pra perguntar antes de avançar.

- [x] 16. Fase 5.1 — Investigação do caminho real de resposta pública do Plano B
  - [x] 16.1 **[API]** Confirmar qual classe monta `planos`/estado de Plano B na resposta pública hoje
    - `AnaliseTransformer::planos[]` (via relacionamento Eloquent `planos`) vs. `AnalysisPublicResponseBuilder`/`evidenceValue('pipeline.execution')` — a auditoria desta sessão não encontrou o segundo padrão no código atual; confirmar antes de escrever qualquer merge
    - Documentar o achado explicitamente nesta task antes de prosseguir
    - **Achado confirmado (2026-09-17), por leitura direta do código, não suposição:** os DOIS padrões existem, cada um servindo uma rota diferente — `AnaliseController::show()` (`GET /analises/{uuid}`, uma análise) usa `AnalysisPublicResponseBuilder` (via `pipeline.execution`) pra toda análise NÃO-legado; `AnaliseTransformer` só entra pra análises `legado` (mesmo endpoint) e pra `index()` (`GET /analises`, a listagem). `AnalysisPublicResponseBuilder::build()` devolvia `execution` 100% do `pipeline.execution` CONGELADO no `evidence_manifest` no instante da decisão — nunca refletia o que `AcompanharPlanos` (comando agendado) grava depois em `genesis_analise_planos` (`status_acionamento`/`desfecho`/`mfe`/`mae`). **Confirma o bug de snapshot congelado que o Requisito 14 descrevia como hipótese** — a tela de UMA análise (o caso que mais importa pra ver se o Plano B já foi acionado) nunca via o estado vivo; só a listagem via, através do `AnaliseTransformer`.
    - _Requisitos: 14.1_
  - [x] 16.2 **[API]** Implementar merge de estado vivo, SE a task 16.1 confirmar um bug de snapshot congelado
    - Estado dinâmico de `genesis_analise_planos` vence a configuração original sem sobrescrever a evidência histórica; expor `trigger.estado`/`triggered_at`/`status_acionamento`
    - **Feito (2026-09-17):** bug confirmado na 16.1 → `AnalysisPublicResponseBuilder::executionComEstadoVivo()` novo — sobrepõe `status_acionamento`/`desfecho`/`mfe`/`mae` vivos (de `Analise::planos()`, carregado sob demanda se não vier eager-loaded) em cima de cada item de `execution.planos[]`, e traduz `status_acionamento` pro vocabulário que `trigger.estado` já usava (`ACIONADO→ATINGIDO`, resto→`AGUARDANDO`) — só a RESPOSTA HTTP muda, `evidence_manifest` persistido nunca é reescrito (testado). **`triggered_at` não implementado** — `genesis_analise_planos` não tem essa coluna hoje (confirmado por leitura do model/migrations); fabricar a partir de `updated_at` seria enganoso (muda em qualquer update do plano, não só no acionamento). Precisaria de migration nova — mesma trava de autorização já documentada nas tasks 12.2/14.1. Fica pendente, documentado, não esquecido.
    - _Requisitos: 14.2_
  - [x] 16.3 **[FE]** Migrar os 7 consumidores de `planoB` para `execution.planos[]`
    - `services/geminiService.ts`, `types.ts`, `types/graphicalAnalysis.ts`, `components/AnalysisResult.tsx`, `components/AnalysisHistoryDashboard.tsx`, e os 2 arquivos de teste — confirmar zero consumidores restantes por grep antes de remover o shape antigo
    - **Feito com um achado real (2026-09-17):** dos 7 arquivos, só `AnalysisResult.tsx` tinha um consumidor de VERDADE ainda ativo — o card seletor do Plano B (entrada/gatilho/estado, 3 usos de `planoB.trigger.estado`/`.entrada`) migrado pra `planoBDados` (`execution.planos[]`, já usado pelo resto do componente desde o hotfix P0.12). **Motivo real de migrar, não só limpeza**: depois da task 16.2, só `planoBDados.trigger.estado` reflete o merge de estado vivo — o `planoB` bruto continuava lendo do snapshot congelado, ficando desatualizado a partir de agora se alguém continuasse nele. `AnalysisHistoryDashboard.tsx` **não precisava de migração** — o `planoB` ali é uma variável local sem relação nenhuma com `execution.planoB` (lê de `item.planos`, o array do `AnaliseTransformer`, que já tinha `desfecho`/`status_acionamento` reais). Os 2 arquivos de teste não tinham consumo real (um já testava `planoBDados`/`gatilhoBPronto` de propósito; o outro só tem `planoB: null` numa fixture `any`, nunca lido). Zero consumidor restante confirmado por grep + `tsc --noEmit` limpo → `ExecutionPlanB` (interface inteira) e o campo `planoB` removidos de `types.ts`/`types/graphicalAnalysis.ts`/`services/geminiService.ts` — o shape antigo não existe mais no frontend. Backend continua podendo mandar a chave (não removida lá nesta sessão, decisão deliberada de não tocar o contrato do backend numa task só de frontend).
    - _Requisitos: 14.3_

- [x] 17. Fase 5.2 — Plano A/B independentes e gatilhos
  - [x] 17.1 **[API]** Garantir que Plano A e B recalculam integralmente ao mudar entrada
    - Entrada/stop/TPs/RR/sizing/margem/liquidação próprios por plano; TP/stop só coincidem entre A/B quando a mesma estrutura real justificar, nunca por conveniência
    - **Confirmado por investigação (2026-09-17), já satisfeito — nenhum código mudou:** leitura de `ExecucaoService::montar()` linha a linha confirma que `$tamanhoB`/`$liquidacaoB`/`$alavancagemInfoB`/`$rrLiquidoB`/`$rrPorAlvoB`/`$riscoPrecoB` são TODOS chamados separadamente com `$planoB['entrada']`/`$planoB['stop']` (nunca reaproveitando as variáveis do Plano A) — `PlanoBService` (reescrito na V6.11) já resolve entrada/stop/TPs 100% independentes. Nenhum copy-by-convenience encontrado.
    - _Requisitos: 14.4_
  - [x] 17.2 **[API]** Implementar máquina de estado por tipo de gatilho em `AcompanharPlanos.php`
    - `ROMPIMENTO` (candle fechado + direção + buffer), `RETESTE` (rompimento confirmado → retorno → teste → sustentação/rejeição), `RETORNO_A_ZONA` (entrada na zona + confirmação + estado persistido) — pavio isolado não satisfaz nenhum dos três
    - **Feito (2026-09-17):** `PlanoTriggerStateMachine.php` novo — os três tipos como funções puras sobre o histórico de candles desde a criação (que `AcompanharPlanos` já busca por inteiro a cada execução, nunca incremental — permite recomputar do zero sem precisar de estado persistido novo, mesma trava de migration das tasks anteriores). ROMPIMENTO exige fechamento além do nível + buffer (`config('genesis.trigger_state_machine.rompimento_buffer_atr')`, default 0,10 ATR); RETESTE encadeia rompimento → retorno à região (`reteste_regiao_atr`, 0,30 ATR) → fechamento de sustentação; RETORNO_A_ZONA exige FECHAMENTO dentro de `zona_de..zona_ate`, nunca só um pavio. `AcompanharPlanos::gatilhoDeclarado()` lê `trigger.tipo`/`trigger.nivel`/`zona_de`/`zona_ate` do snapshot congelado (`evidence_manifest['pipeline.execution'].planos[]`) — nenhum desses campos é coluna de `genesis_analise_planos` hoje. Fallback pro toque de pavio antigo (`indiceAcionamentoPorToque()`) preservado só para planos sem `trigger.tipo` estruturado (análises anteriores a este item).
    - _Requisitos: 14.5_
  - [x] 17.3 **[API]** Garantir que Plano A e B são sempre salvos e acompanhados, independente da escolha do membro
    - **Confirmado por investigação (2026-09-17), já satisfeito — nenhum código mudou:** `AnalysisPersistenceService::persistPlanos()` itera `$execution['planos']` incondicionalmente — `plano_escolhido` (setado depois, só quando o membro clica um plano) nunca influencia quais linhas de `AnalisePlano` são criadas. Confirmado também pelos testes já existentes (`GraphicalAnalysisOrchestratorPlanoPersistenceTest`).
    - _Requisitos: 14.6_
  - [x]* 17.4 Testes de Plano B e gatilhos
    - Estado vivo chega ao frontend (ou confirma que já chegava); rompimento exige rompimento; reteste exige sequência completa; retorno à zona exige retorno; toque de pavio não substitui nenhuma semântica
    - **Feito (2026-09-17):** `PlanoTriggerStateMachineTest` (12 testes unitários: pavio isolado nunca confirma nenhum dos três tipos, fechamento com/sem buffer, sequência completa de RETESTE e cada etapa faltando, fechamento dentro/fora da zona). `AcompanharPlanosTest` ganhou 2 testes diferenciais provando que a MÁQUINA DE ESTADO é usada (não o toque de pavio antigo) — `entrada` fixada no preço atual (que sob a regra antiga acionaria quase certamente) + `trigger.nivel`/zona inatingíveis → plano precisa continuar PENDENTE.
    - _Requisitos: 14.2, 14.5_

- [x] 18. Fase 5.3 — Targets e RR
  - [x] 18.1 **[API]** Criar `TargetEligibilityService.php`
    - Recebe `target_ranking` (até 6 IDs), entrada, stop final; calcula RR por candidato; aplica `rr_minimo`; aplica espaçamento ATR; publica até 3; mantém rejeitados como barreiras de contexto
    - **Feito (2026-09-17):** `TargetEligibilityService.php` novo — filtra o ranking (lado, limites ATR, `RR >= config('genesis.rr_minimo')`), publica até 3 em ordem CRESCENTE de distância com espaçamento guloso >= 0,50 ATR (a ordem do ranking é só preferência, nunca a ordem final). `GenesisDecisionSchema.php`: `target_selection` (até 3, ordem final) renomeado/ampliado pra `target_ranking` (até 6, preferência) — mesma mudança espelhada em `plan_b.target_ranking`. Versão sobe pra `v7.1.0` (schema+prompt), `VersionamentoSincronizadoTest` trava a sincronia. `GenesisPrompt.php` reescrito: a IA ordena por preferência, não decide mais quantos cabem nem o espaçamento. `ExecucaoService::montar()`/`PlanoBService::gerar()` chamam `TargetEligibilityService` logo após o stop de cada plano ser resolvido (nunca antes — RR precisa do stop final), então alimentam `AlvoService::calcularAlvos()` só com os ids já filtrados.
    - _Requisitos: 15.1_
  - [x] 18.2 **[API]** Retirar `TargetCandidateCatalog::strength` do caminho de decisão
    - Confirmado em uso hoje como filtro (`> 0.15`) — remover esse filtro do caminho V2; `strength` pode continuar existindo só para telemetria/legado
    - **Feito com escopo deliberado (2026-09-17):** o uso real de "strength como caminho de decisão" era o PROMPT instruindo a IA a tratar `strength` como "critério principal" pra escolher alvos (`GenesisPrompt.php`, linha removida) — corrigido: a seção de seleção de alvo agora aponta origem/confluência/coerência técnica, nunca `strength`. `TargetEligibilityService` (o novo gate real de elegibilidade) nunca referencia `strength`. **Não removido:** o campo `strength` continua no objeto de cada candidata (usado depois, pós-decisão, por `ExecucaoService::forcaCandidato()`/`QualidadeEntradaService` — telemetria de qualidade de entrada, uso sancionado pela Fonte) e o cap de ruído `limitarFracas()` (4 candidatas fracas por lado) continua ativo — é uma checagem de volume de prompt, não de elegibilidade de TP, fora do que o Requisito 15.2 pede. Bifurcar `target_candidates` em duas variantes (uma sem `strength` pro prompt, outra completa pro pós-decisão) resolveria a letra mais literal ("não enviá-lo") mas exigiria duplicar um array hoje compartilhado entre o bundle da IA e o cálculo pós-decisão — maior que o que o critério de aceite pede; documentado como decisão de escopo, não esquecimento.
    - _Requisitos: 15.2_
  - [x] 18.3 **[API]** Separar `target_candidates_all` de `target_candidates_tp_eligible`
    - Nível real abaixo do RR mínimo continua como barreira de contexto (`tp_eligible=false`), nunca apagado
    - **Feito (2026-09-17):** `TargetEligibilityService::selecionar()` devolve `all` (TODO candidato real do catálogo, decorado com `tp_eligible`/`rr`/`motivo_rejeicao` — `LADO_ERRADO`/`FORA_DOS_LIMITES`/`FORA_DO_RANKING_IA`/`RR_ABAIXO_DO_MINIMO`/`ESPACAMENTO_ATR_INSUFICIENTE`) além de `published_ids`. `ExecucaoService`/`PlanoBService` expõem os dois novos campos por plano: `target_candidates_all` (= `all`) e `target_candidates_tp_eligible` (= subconjunto `tp_eligible=true`) — sem migration nenhuma, calculados a cada resposta, não persistidos como coluna nova.
    - _Requisitos: 15.3_
  - [x] 18.4 **[API]** Refatorar `TargetSelectionValidator.php` para assert final
    - Não exigir número exato de targets; validator só confirma que o publicado respeita contrato
    - **Feito (2026-09-17):** `validate()` virou dois métodos. `validateRanking()` roda em `DecisionResponseValidator` sobre o `target_ranking` bruto — só confere o que é erro DA IA (id existe, lado certo, sem duplicata, limites ATR, rationale alinhado, elementos reais); não confere mais contagem nem espaçamento (a IA não sabe o stop final ainda). `assertPublished()` é o assert final de verdade — roda sobre o resultado JÁ PUBLICADO por `TargetEligibilityService` (até 3, ordem, espaçamento, limites); por construção deveria sempre passar — uma falha aqui é bug interno do selecionador, nunca motivo de repair.
    - _Requisitos: 15.5_
  - [x] 18.5 **[API]** Remover conceito de RR combinado de qualquer caminho vivo em `ExecucaoService`/`ExecutionPipelineService`
    - RR1/RR2/RR3 individuais, deterministicos, recalculados ao mudar entrada/stop
    - **Confirmado por investigação (2026-09-17), já satisfeito — nenhum código mudou:** grep por "rr_combinado"/variações no `app/` não encontrou nenhuma ocorrência — o hotfix V6.11 final (P0.10, citado no próprio `config/genesis.php`) já tinha removido `rr_liquido_combinado`/`parciais_alvo` do caminho vivo antes desta sessão. `calcularRrPorAlvo()` já calcula RR1/RR2/RR3 individualmente, sempre recalculado a partir de entrada/stop atuais (confirmado também pelo endpoint de repricing, task 14).
    - _Requisitos: 16.1, 16.2_
  - [x]* 18.6 Testes de targets e RR
    - Nível a 0,14R continua barreira mas não vira TP com `rr_minimo=1.5`; 0/1/2/3 TPs publicáveis; nunca exige 3; `TARGET_SELECTION_TOO_CLOSE_TO_PREVIOUS` pós-selector tratado como bug interno, não repair normal
    - **Feito (2026-09-17):** `TargetEligibilityServiceTest` (9 testes: publica em ordem crescente de distância ignorando a ordem do ranking, RR abaixo do mínimo excluído mas preservado em `all` com motivo, espaçamento insuficiente rejeita o mais distante do par, lado errado, candidato fora do ranking mas real continua em `all`, máximo 3 mesmo com ranking de 6, sem stop não filtra por RR mas ainda filtra lado/espaçamento, ranking vazio, fora dos limites de distância). `TargetSelectionValidatorTest` reescrito — metade pra `validateRanking()` (sem mais teste de contagem/ordem, que não é mais responsabilidade dela; ganhou teste explícito de que ranking fora de ordem/com menos de 3 NÃO é erro), metade nova pra `assertPublished()` (>3 vira `TARGET_SELECTION_INTERNAL_COUNT_EXCEEDS_THREE`, fora de ordem/colado/lado errado/fora dos limites viram os equivalentes `_INTERNAL_*`, nunca os códigos antigos que disparavam repair). `ExecutionExecutableVsRecommendedTest::test_e02_rr_baixo...` reescrito — o estado antigo que testava ("TP1 real só que com RR ruim, publicado com aviso") deixou de existir por construção; agora prova que o candidato é FILTRADO antes da publicação (`SEM_BARREIRA_REAL`, mesmo desfecho do teste e01) e continua em `target_candidates_all` com `tp_eligible=false`.
    - **Valida: Propriedade 9, Propriedade 10 (design.md)**
    - _Requisitos: 15.4, 15.5_

- [x] 19. Checkpoint Fase 5 — Plano A/B, targets e RR completos
  - Rodar suíte completa de testes de plano/target/RR
  - Perguntar ao usuário se há dúvidas antes de avançar para a Fase 6
  - **Feito (2026-09-17):** suíte completa do backend rodada DUAS vezes após as tasks 17/18 (contrato de decisão renomeado/ampliado — `target_selection`→`target_ranking`, schema v7.1.0 — tocou ~23 arquivos de teste que construíam fixtures de decisão). Primeira rodada achou 2 quebras reais além do rename mecânico: `GenesisPromptRrProvisorioTest` (uma asserção esperava `strength` fora da lista de campos da candidata no prompt — restaurado, `strength` continua um campo real do JSON, só não é mais instruído como critério; a task 18.2 nunca pediu removê-lo do payload) e `V69CriticalInvariantsTest` (chamava `TargetSelectionValidator::validate()`, o nome antigo do método antes do split da task 18.4). Os dois corrigidos; segunda rodada: 1192 passaram, 4 falhas, todas pré-existentes e confirmadas não-regressão por reexecução isolada — 3 são a MESMA race de `queue:work --stop-when-empty` sob carga já documentada nas tasks 9-16 desta sessão (`GraphicalAnalysisAttemptJobTest` ×2, `GraphicalAnalysisFullPipelineIntegrationTest`), a 4ª é a colisão transiente de `users.email` em factory sob execução rápida repetida, já documentada como não-reprodutível nesta mesma sessão (`CreditReservationServiceGenesisAuthTest`, sem relação nenhuma com targets/planos/gatilhos). Usuário já tinha direcionado "execute 17,18 e 19" na mesma instrução — não houve pausa pra perguntar antes de avançar, mesmo padrão das tasks 15/16.

- [x] 20. Fase 6 — Alavancagem/bracket real e liquidação
  - [x] 20.1 **[API]** Confirmar `GENESIS_BINANCE_API_KEY`/`GENESIS_BINANCE_API_SECRET` configuradas e `getLeverageBrackets()` funcionando em produção
    - **Bloqueado (2026-09-17), mesmo padrão da task 1.2:** confirmado por leitura direta (`config('binance.api_key')`/`config('binance.api_secret')` em runtime) que este sandbox NÃO tem as credenciais configuradas — `getLeverageBrackets()` sempre devolve `null` aqui (retorno antecipado por credencial ausente, `BinanceService.php`, achado real já documentado em produção 11/09/2026). Impossível confirmar de dentro deste ambiente se o endpoint assinado (`/fapi/v1/leverageBracket`) funciona contra a Binance real — precisa ser verificado em produção/staging com as credenciais reais.
    - _Requisitos: 18.1_
  - [x] 20.2 **[API]** Usar `maintMarginRatio` real do bracket por nocional em `LiquidationCalculatorService.php`
    - Só remover `MM_TIER1_FALLBACK`/`MM_TIER1_FALLBACK_PADRAO`/`MATH_ESTIMATE_TIER1` depois de confirmar que o bracket real funciona para os contratos em uso
    - **Bloqueado + conflito documentado, nenhum código tocado (2026-09-17):** a pré-condição da própria task ("só remover depois de confirmar que o bracket real funciona") não pode ser satisfeita aqui — 20.1 está bloqueado. Além disso, achei uma divergência real e deliberada entre a Fonte e uma decisão de produto já embarcada: o código atual NÃO cai em `UNAVAILABLE` quando o bracket real falta — cai em `MATH_ESTIMATE_TIER1` (manutenção estimada por símbolo, `source` explicitamente distinto de `BINANCE_LEVERAGE_BRACKET`), uma correção datada (11/09/2026, docblock da própria classe) pra um reclamo real de cliente ("o valor da liquidação não vem"), porque a v2api nunca teve as credenciais configuradas. Mesmo padrão de conflito Fonte-vs-decisão-já-confirmada já documentado nesta sessão (`config/genesis_graphical_v6.php`, sobre `GENESIS_MODEL`/`gemini-3.6-flash` vs `gemini-3.7-flash`) — não revertido sem confirmação explícita do Felipe.
    - _Requisitos: 18.2_
  - [x] 20.3 **[API]** Se bracket real indisponível, `liquidation.status=UNAVAILABLE`, logar, nunca fabricar manutenção estimada
    - **Mesmo bloqueio/conflito da 20.2 (2026-09-17):** já loga (`GENESIS_LIQUIDACAO_BRACKET_INDISPONIVEL`, `LiquidationCalculatorServiceTest::test_brackets_vazios_sem_excecao_loga_indisponibilidade_e_ainda_estima` prova isso), só não usa `UNAVAILABLE` como status final — usa `ESTIMATED`, pela mesma decisão datada acima. Não alterado.
    - _Requisitos: 18.3_
  - [x] 20.4 **[API]** Validar alavancagem contra bracket real no backend; **[FE]** frontend só oferece alavancagens compatíveis com o contrato
    - **Feito (2026-09-17):** `BinanceService::maxLeverageFor()` novo — maior `initialLeverage` entre os tiers reais do símbolo (`null` sem bracket real, nunca inventa teto). Backend: `GraphicalAnalysisRequest::withValidator()` (criação de análise) e `AnaliseController::reprice()` (task 14) rejeitam (422) alavancagem acima do teto real quando ele está disponível — nunca bloqueiam quando indisponível (mesma doutrina de degradação graciosa do resto do V2). Novo `GET /v1/leverage-brackets/{symbol}` (`LeverageBracketController`) expõe o teto pro frontend. Frontend: `services/api.ts::getLeverageBracket()` + `AppContext.tsx` — novo estado `binanceMaxLeverage` (só consultado quando `exchange==='Binance'`, já que a análise em si sempre roda contra Binance USD-M) estreita `leverageOptions` (a lista fixa por exchange que já existia) via `effectiveLeverageOptions` memoizado; nunca amplia, nunca esvazia o seletor. Mesma disciplina de aviso já existente (B-22) quando o corte força a leitura atual pra baixo.
    - _Requisitos: 17.3_
  - [x]* 20.5 Testes de alavancagem e liquidação
    - Alterar leverage não gera nova decisão; leverage inválido rejeitado; bracket real usado; sem bracket → `UNAVAILABLE`; nenhum fallback 0.004/0.005 sobrevive em produção V2
    - **Feito, com a ressalva de 20.2/20.3 acima (2026-09-17):** `BinanceServiceMaxLeverageForTest` (5 testes), `LeverageBracketControllerTest` (4), `GraphicalAnalysisLeverageBracketValidationTest` (3, incluindo prova de que a rejeição acontece ANTES de gastar crédito) e 2 novos em `RepricingEndpointTest`. "sem bracket → UNAVAILABLE"/"nenhum fallback 0.004/0.005 sobrevive" não se aplicam como descritos — ver conflito documentado em 20.2/20.3, comportamento real já coberto por `LiquidationCalculatorServiceTest` (pré-existente). "Alterar leverage não gera nova decisão" já garantido desde a task 14 (endpoint de repricing, matemática pura).
    - **Valida: Propriedade 5, Propriedade 7 (design.md)**
    - _Requisitos: 17.3, 18.2, 18.3_

- [x] 21. Fase 7.1 — Macro/sentimento fora do bundle decisório (confirmação + teste formal)
  - [x] 21.1 **[API]** Confirmar que `unset($decisionManifest['context'])`/`unset($bundle['context'])` em `CanonicalBundleBuilder` cobrem 100% dos caminhos que chegam a `DecisionProvider::decide()`
    - **Confirmado por investigação (2026-09-17), verdicto: exclusão 100% real e completa, nenhuma lacuna encontrada.** `CanonicalBundleBuilder::build()` monta `$bundle['context']` (visão interna completa), mas `manifest_hash` já é calculado sobre um clone sem ele desde a Fase 2 (item 2.8); `forStage1()` — o único método que produz o que de fato chega em `DecisionProvider::decide()` (`GraphicalAnalysisAttemptJob::handle()`, chamada direta, sem transformação intermediária) — faz `unset($bundle['context'])` explícito antes de `BrainBundleGuard::stripDisplayOnly()`/`assertDecisionSafe()`. `EvidenceCatalog`/`EvidenceManifestBuilder` confirmam que nenhum item `macro.*`/`sentiment.*` sequer existe no catálogo de evidência decisória (removidos de fase anterior). Teste pré-existente (`CanonicalBundleBuilderStage1Test::test_remove_context_do_bundle_e_do_json`) já cobria isso no nível de `forStage1()` isolado.
    - _Requisitos: 20.1_
  - [x] 21.2 **[API]** Confirmar que o `$snapshot['macro']`/`$snapshot['sentiment']` usado no manifesto de evidências/narrativa pública nunca reintroduz contexto no payload decisório
    - **Confirmado junto com 21.1 (2026-09-17):** `AnalysisPublicResponseBuilder::macroContext()`/`sentimentContext()` leem exclusivamente de `Analise::context_payload` (coluna separada, populada por `ContextProvider::collect()`+`InformativeDisplayContextService::collect()` dentro do job), nunca de `bundle`/`evidence`/`$built` — caminho fisicamente distinto do que chega em `decide()`.
    - _Requisitos: 20.2_
  - [x]* 21.3 Criar teste de regressão formal
    - Asserção de que o payload enviado a `GeminiDecisionClient`/`OpenAiDecisionClient` nunca contém chave `context`/`macro`/`sentiment`/`sentimento`
    - **Feito (2026-09-17):** `GraphicalAnalysisAttemptJobTest::test_payload_enviado_ao_decisor_nunca_contem_context_macro_ou_sentimento` — captura o corpo HTTP REAL enviado a `api.openai.com` (`Http::fake()` com closure, não só mocka a resposta) através do fluxo completo do job, não `forStage1()` isolado; confere ausência das 4 chaves como pares `"chave":` (não a palavra solta — o próprio prompt do decisor MENCIONA "macro"/"sentimento" em prosa, instruindo a IA a nunca receber esse contexto; só a CHAVE de objeto real não pode existir).
    - _Requisitos: 20.1_

- [x] 22. Fase 7.2 — Persistência e histórico recuperável por UUID
  - [x] 22.1 **[API]** Confirmar/expor `GET /v1/analises/{uuid}`
    - **Confirmado por investigação (2026-09-17), já satisfeito — nenhum código mudou:** `routes/genesis_graphical_v6.php` já expõe `GET /api/v1/analises/{id}` (`AnaliseController::show()`, resolve por UUID ou id numérico legado); `AnaliseUuidResolutionTest` (pré-existente) já prova os dois caminhos.
    - _Requisitos: 25.1_
  - [x] 22.2 **[FE]** Criar rota `/genesis/analise/:uuid` que restaura a análise a partir do servidor
    - Não depender só de state React
    - **Feito (2026-09-17):** investigação prévia confirmou que o app usa React Router v7 (`router/index.tsx`), sem nenhuma rota de análise única existente — `GenesisPage.tsx` cria E exibe inline, sem persistência nenhuma além do state do `AppContext` (perdido em qualquer remount/refresh). Nova rota `dashboard/genesis/analise/:uuid` (mesmo componente `GenesisPage`, convenção de layout/auth existente — a Fonte cita `/genesis/analise/:uuid` sem o prefixo `/dashboard`, mas a app inteira já organiza suas telas assim, mesmo padrão da correção de prefixo `/api` -> `/api/v1` da task 0.2). `GenesisPage` lê `useParams().uuid`; se presente e diferente da análise já carregada, busca via `getAnaliseByUuid()` (novo, `services/api.ts`, `GET /v1/analises/{uuid}`) e popula `AppContext` com `mapGraphicalToLegacy()` (exportado de `geminiService.ts`, a mesma tradução que o polling da criação já usa — nunca duplicada). **Escopo deliberado:** só restaura quando `status==='COMPLETED'`; uma análise ainda PENDING no instante do acesso não reabre o polling completo (reabrir a máquina de estado de `analyzeChart()` aqui duplicaria aquele fluxo inteiro por um caso raro — a maioria dos acessos acontece bem depois do processamento terminar).
    - _Requisitos: 25.1, 25.2_
  - [x] 22.3 **[FE]** Garantir que navegar para o Radar e voltar, ou dar refresh, mantém a mesma análise aberta
    - **Feito (2026-09-17):** navegar dentro da SPA já preservava o state (o `AppContext` é montado uma vez, no root) — o que faltava era sobreviver a um REMOUNT completo (refresh, link direto). `GenesisPage.tsx`: ao concluir uma análise nova, `navigate()` move a URL pra `/dashboard/genesis/analise/{uuid}` (`replace: true` — mesma visão, não uma entrada nova no histórico); `handleResetAnalysis()` volta pra `/dashboard` ao fechar. Histórico: `AnaliseTransformer.php` ganhou `analysis_uuid` (nunca exposto antes, só o id numérico interno) — `AnalysisHistoryDashboard.tsx` agora navega pra a análise completa ao clicar numa linha (`stopPropagation()` nos botões internos de ACERTOU/ERROU, pra não disparar a navegação junto).
    - _Requisitos: 25.2_
  - [x] 22.4 **[API]** Persistir `brain_version`, `prompt_version`, `schema_version`, `provider`, `model`, `bundle_hash` em toda análise
    - **Confirmado por investigação (2026-09-17) — 5 de 6 já satisfeitos sob nomes diferentes, 1 pendente de migration:** `brain_version`≈`analysis_version` (= `document_version`, 'V2'), `prompt_version`/`schema_version` (mesmo nome), `model`≈`model_id`, `bundle_hash`≈`manifest_hash` (já é o hash do bundle canônico real, não do screenshot — confirmado no código de `CanonicalBundleBuilder::build()`, e agora ATIVAMENTE consumido pela task 22.5 abaixo). **`provider` não é uma coluna dedicada** — existe hoje só dentro de `provider_telemetry->decision->provider` (JSON). Adicionar a coluna exigiria migration nova — mesma trava de autorização já documentada repetidamente nesta sessão (tasks 12.2/14.1/16.2). Fica pendente, documentado, não esquecido; `provider_telemetry` já é suficiente pra auditoria manual enquanto isso.
    - _Requisitos: 21.2_
  - [x] 22.5 **[API]** Implementar reuso de decisão por `bundle_hash` idêntico
    - Hash reflete o bundle canônico real (facts + derived + vision + availability), não só o screenshot
    - **Feito (2026-09-17):** `GraphicalAnalysisAttemptJob::handle()` — antes de chamar `DecisionProvider::decide()` (só na 1ª tentativa, `repair===null`; um repair nunca pula pra decisão de outra análise), procura uma `Analise` `COMPLETED` com o mesmo `manifest_hash` E `schema_version` (evita queimar uma tentativa contra uma decisão de contrato antigo, que a validação rejeitaria de qualquer forma) e reusa `decision_payload` dela — a decisão reusada passa pelo MESMO pipeline de validação/reparo mecânico/normalização de texto de sempre (nunca um atalho que pula validação), já que o bundle é comprovadamente idêntico. `cache_hit=true` só quando reusado. Escopo deliberado: reuso é GLOBAL (não restrito ao mesmo usuário) — a decisão é função só do bundle de mercado+imagem, nunca de identidade de usuário; dado que `manifest_hash` inclui timestamps de observação exatos, colisão real só acontece dentro da mesma janela de cache de mercado (o cenário que a Fonte descreve — "nada mudou").
    - _Requisitos: 22.1, 22.2_
  - [x]* 22.6 Testes de persistência
    - Navegar e voltar mantém análise; refresh mantém análise; UUID recupera análise; histórico lista sem duplicar linha
    - **Feito (2026-09-17):** backend — `test_bundle_hash_identico_reusa_decisao_persistida_sem_chamar_a_ia_de_novo` (a segunda chamada não tem NENHUMA rota HTTP configurada; se o job tentasse chamar a IA de novo, a chamada estouraria) + `test_sem_analise_anterior_com_mesmo_hash_chama_a_ia_normalmente` (caminho feliz sem reuso continua intacto). Frontend — `getAnaliseByUuid.test.ts` (3 testes: busca com auth, `null` em 404, uuid codificado na URL). "Navegar e voltar"/"refresh mantém análise" são comportamento de roteador (React Router `createBrowserRouter` + o efeito de restauração) — verificados por leitura de código e `tsc --noEmit` limpo, não por um teste de montagem de componente real (este repo não tem harness de render de página inteira; os testes frontend existentes são unitários/property-based sobre módulos, nunca End-to-end de UI). "Histórico lista sem duplicar linha" já garantido desde a V6.5 (F03-F04, comentário no topo de `AnalysisHistoryDashboard.tsx`), não uma garantia nova desta fase.
    - _Requisitos: 25.3_

- [x] 23. Fase 7.3 — Outcomes como timeline
  - [x] 23.1 **[API]** Substituir o `foreach`/`return` de primeiro-evento-terminal em `DesfechoService.php` por timeline cumulativa
    - `ENTRY_TRIGGERED`, `TP1_HIT`, `TP2_HIT`, `TP3_HIT` ou `STOP_HIT_AFTER_TPn`, `EXPIRED`; nunca encerrar em TP1
    - **Feito (2026-09-17):** `DesfechoService::avaliar()` reescrito — percorre TODOS os candles disponíveis (nunca retorna no primeiro TP), acumulando uma `timeline` (`ENTRY_TRIGGERED`/`TPn_HIT`/`STOP_HIT`[`_AFTER_TPn`]/`AMBIGUOUS_INTRABAR`, vocabulário literal da Fonte §74) e um novo `concluido` (bool) — só `true` em stop, TP3 ou ambiguidade sem resolução; TP1/TP2 isolados devolvem `concluido=false`. `resultado`/`preco_resultado` (vocabulário legado `TP1_ATINGIDO`.../`STOP_ATINGIDO`/`AMBIGUO_MESMA_VELA`/`PENDENTE`) preservados 100% — só passam a poder ser sobrescritos em execuções seguintes conforme a trajetória avança, em vez de travar no primeiro valor. `AcompanharPlanos::handle()`: query trocou `whereNull('desfecho')` por `where('encerrado', false)` (coluna nova); `fecharDesfechoNaAnalise()` (propaga pra `genesis_analises.resultado`) agora gateada por `$resultado['concluido']`, não mais por "resultado não é PENDENTE" — TP1/TP2 isolados não fecham mais o resultado da análise por engano. Timeline persistida em tabela nova `genesis_analise_plano_eventos` (`AnalisePlanoEvento`, `firstOrCreate` por `(analise_plano_id, evento)` — idempotente entre execuções). **Achado real fora do escopo original, corrigido:** `genesis_analises.resultado` é um ENUM real em MySQL (`TP1_ATINGIDO`/`TP2_ATINGIDO`/`TP3_ATINGIDO`/`STOP_ATINGIDO`/`AMBIGUO_MESMA_VELA`/`PENDENTE`) que NUNCA incluiu `'EXPIRADO'`, embora `AcompanharPlanos` grave esse valor ali desde V6.7/E-37 quando um Plano B escolhido expira sem ser acionado — em MySQL com `sql_mode` estrito isso lançaria `Data truncated for column 'resultado'` de verdade em produção (mascarado em teste porque SQLite não impõe ENUM). Migration nova adiciona `'EXPIRADO'` ao enum, mesmo padrão da migration que já tinha feito isso para `AMBIGUO_MESMA_VELA`.
    - _Requisitos: 26.1_
  - [x] 23.2 **[API]** Calcular e persistir `MFE_R`/`MAE_R`/`MFE_ATR`/`MAE_ATR` (bruto e normalizado)
    - **Feito (2026-09-17):** `AcompanharPlanos::handle()` calcula `R = |entrada-stop|` e lê o ATR congelado (`volatility.atr14` do `evidence_manifest`, helper `atrDaAnalise()` extraído do que já existia só para Plano B em `gatilhoDeclarado()` — agora compartilhado pelos dois planos) a cada execução, dividindo `mfe`/`mae` (já existentes, em preço bruto) por R e por ATR. 4 colunas novas em `genesis_analise_planos` (`mfe_r`/`mae_r`/`mfe_atr`/`mae_atr`, nullable — nunca fabricadas quando R ou ATR não dá pra determinar).
    - _Requisitos: 26.2_
  - [x] 23.3 **[API]** Usar granularidade de 1m para resolver ordem intrabar stop-vs-TP onde viável
    - Registrar `AMBIGUOUS_INTRABAR` quando impossível resolver, nunca inventar ordem
    - **Feito (2026-09-17):** `DesfechoService::avaliar()` ganhou um `?callable $resolverIntrabar` opcional — quando stop e um TP novo tocam no mesmo candle, tenta refinar via candles de granularidade menor antes de declarar ambiguidade (recursão sobre a mesma lógica, sem refinar de novo — 1m já é o piso); só aceita o refinamento se ele decidir algo terminal de verdade, nunca propaga um resultado parcial. `AcompanharPlanos::resolverIntrabarEm1m()` é o único produtor real — busca candles de 1m da Binance só para o período EXATO do candle ambíguo (open→close, nunca a janela inteira do acompanhamento, que estouraria os 1000 candles/chamada da Binance em timeframes longos); se o candle ambíguo já é de 1m, ou a Binance falhar/devolver vazio, devolve `null` e o resultado fica `AMBIGUO_MESMA_VELA`/`AMBIGUOUS_INTRABAR`. `ResultVerifierCommand` (legado, análises pré-V6.5) deliberadamente NÃO ganhou o resolvedor — fora do escopo desta fase (Plano A/B), mesmo padrão de fronteira já usado nesta sessão.
    - _Requisitos: 26.3_
  - [x] 23.4 **[API]** Estender `EvaluateGenesisOutcomes.php` com vínculo a `score`/`brain_version`
    - Manter horizontes 60/240/1440 min já existentes; permitir comparação legado x V2
    - **Feito (2026-09-17):** 2 colunas novas em `genesis_analysis_outcomes` (`score`/`brain_version`, denormalizadas de `Analise::score`/`analysis_version` no instante da avaliação) — o vínculo já existia via `analysis_id` (FK), a denormalização evita JOIN em todo relatório legado-vs-V2 (a própria Fonte §78 lista "score x resultado" como consulta direta do dashboard futuro). Horizontes 60/240/1440 intocados.
    - _Requisitos: 26.4_
  - [x] 23.5 **[API]** Garantir imutabilidade da análise original
    - `decision_at_analysis`/`plan_A_original`/`plan_B_original`/`stop_recommended_original`/`targets_original` nunca reescritos; eventos ficam em tabela/timeline separada
    - **Confirmado por investigação (2026-09-17), já satisfeito — nenhuma mudança de código:** `Analise::decision_payload` (=`decision_at_analysis`) e `Analise::evidence_manifest` (=`market_snapshot_at_analysis`+`plan_A_original`+`plan_B_original`+`stop_recommended_original`+`targets_original`, todos dentro de `evidence_manifest['pipeline.execution'].planos[]`) só são escritos UMA VEZ, por `AnalysisPersistenceService::computeAttributes()`, chamado de um único ponto (`persistIntoExisting()`, `GraphicalAnalysisAttemptJob::handle()`) protegido por um guard de idempotência (`analysis_status` já terminal → `return` antecipado, nunca reprocessa). Confirmado especificamente que o reuso por bundle-hash da task 22.5 só LÊ `decision_payload` da análise antiga (nunca `->update()`/`->save()` nela) e escreve na análise NOVA. `AnalisePlano.entrada/stop/tp1/tp2/tp3` também só são escritos na criação (`persistPlanos()`); `AcompanharPlanos` só atualiza `status_acionamento`/`desfecho`/`mfe`/`mae`/`encerrado`/`mfe_r`/`mae_r`/`mfe_atr`/`mae_atr` — nunca os campos geométricos originais. `RepricingService` (Fase 4.3) confirmado sem NENHUM `->update()`/`->save()`/`->create()` — só leitura+matemática, já provado por teste (`test_reprice_nunca_persiste_nada_na_analise_original`).
    - _Requisitos: 26.5_
  - [x] 23.6 **[API]** Separar `stop_recommended` de `stop_user_adjusted` no armazenamento de outcome
    - **Feito com um recorte deliberado (2026-09-17):** `AnalisePlano.stop` já É `stop_recommended` — imutável por construção (confirmado pela 23.5: reprice nunca persiste). `stop_user_adjusted` (coluna nova, nullable) fica reservada: não existe hoje NENHUMA ação real de "salvar o stop que o membro ajustou no slider" (o slider/reprice da Fase 4.2/4.3 são só preview, nunca persistem) — o campo é a separação estrutural que a Fonte §80 pede, pronta para quando essa ação existir, sempre `null` até lá. Mesmo padrão de campo-pronto-sem-produtor já usado nesta sessão (`triggered_at`, task 16.2; `provider`, task 22.4).
    - _Requisitos: 26.6_
  - [x]* 23.7 Testes de outcomes
    - A e B sempre monitorados; TP1 não encerra tracking; TP2/TP3 registrados; MFE_R/MAE_R/MFE_ATR/MAE_ATR calculados; ambiguidade intrabar registrada, não inventada
    - **Feito (2026-09-17):** `DesfechoServiceTest` ganhou 8 testes novos (TP1 isolado não encerra; timeline acumula TP1→TP2 em candles separados; um candle que pula direto até TP2 registra os dois `_HIT`; stop após TP1 vira `STOP_HIT_AFTER_TP1` mas `resultado` legado continua `STOP_ATINGIDO`; TP3 encerra; refinamento em 1m desambiguando corretamente; refinamento sem conseguir decidir ainda vira `AMBIGUOUS_INTRABAR`; sem resolvedor, comportamento idêntico ao antigo). `AcompanharPlanosTimelineTest.php` novo (5 testes, Binance mockada — diferente do `AcompanharPlanosTest.php` pré-existente, que usa preço real e nunca exercita um desfecho de verdade): TP1 isolado não sai da fila (`encerrado=false`); plano reprocessado em execuções seguintes progride até TP2 sem duplicar eventos (idempotência), com `mfe_r`/`mfe_atr` numericamente conferidos; stop após TP1 encerra e fecha `genesis_analises.resultado`; candle ambíguo resolvido de verdade com uma segunda chamada real à Binance em `1m` (wiring completo de `resolverIntrabarEm1m()`); candle ambíguo sem dado de 1m disponível fica `AMBIGUO_MESMA_VELA` e encerra (nunca fica preso reavaliando pra sempre). "A e B sempre monitorados" já coberto por `AcompanharPlanosTest.php` pré-existente + confirmação da task 17.3 desta sessão.
    - **Valida: Propriedade 8 (design.md)**
    - _Requisitos: 26.1, 26.2, 26.3, 26.7_

- [x] 24. Checkpoint Fase 7 — Persistência, histórico e outcomes completos
  - Rodar suíte completa de testes de persistência/outcomes
  - Perguntar ao usuário se há dúvidas antes de avançar para a Fase 8
  - **Feito (2026-09-17):** suíte completa do backend rodada (1222 passaram, 5 falhas na primeira passada) — todas as 5 falhas reconfirmadas como as MESMAS duas categorias de flake já documentadas repetidamente nesta sessão (`queue:work --stop-when-empty` sob carga; colisão transiente de `users.email` em factory), reproduzido 0/5 numa segunda rodada isolada dos mesmos arquivos (depois 0/29 falhas juntos, depois cada arquivo sozinho 100% limpo) — nenhuma relacionada a `DesfechoService`/`AcompanharPlanos`/`EvaluateGenesisOutcomes`/`AnalisePlano`/`AnalysisOutcome`. **Achado incidental corrigido:** a primeira rodada completa também pegou uma linha órfã real na tabela `jobs` do `database/testing.sqlite` (um job de teste nunca limpo de uma sessão anterior, `attempts=0`, sem relação com esta fase) fazendo `GraphicalAnalysisAsyncFlowTest` falhar por contagem de lixo na fila — removida (dado descartável de infraestrutura de teste local, não um achado de produto). Usuário já tinha direcionado "execute 23, 24 e 25" na mesma instrução — não houve pausa pra perguntar antes de avançar, mesmo padrão de checkpoints anteriores.

- [x] 25. Fase 8.1 — Benchmark final e teste do legado morto
  - [x] 25.1 **[API]** Rodar benchmark 20x do bundle real congelado contra o contrato V2 completo (schema/prompt novos)
    - Zero flip LONG↔SHORT bloqueia release; variação de score > 10 pontos exige fallback categórico estável (`MARGINAL→55`...`MUITO_FORTE→85/90`) antes de liberar score livre
    - **Bloqueado, com achado novo (2026-09-17):** as credenciais OpenAI deste sandbox voltaram a funcionar (mudança real desde a task 1.2/20.1, que achou `insufficient_quota` em 16/09) — confirmado com 2 chamadas reais de sondagem (`genesis:benchmark-decision --runs=1`), que chegaram a respostas HTTP de verdade da OpenAI. Mas o achado que bloqueia de fato: o único comando de benchmark existente (`genesis:benchmark-decision`/`BenchmarkGenesisDecision.php`) está **deliberadamente fixado no contrato LEGADO V6.7** (`CanonicalBundleBuilderV67Baseline`/`DecisionResponseValidatorV67Baseline`, por design explícito de uma spec anterior de comparação antigo-vs-novo, docblock próprio confirma) — não mede o contrato V2 (schema `decision-v7.1.0`) que esta task pede, mesmo com credenciais funcionando. Rodá-lo não provaria o que a task pede. Perguntei ao Felipe como proceder (construir uma ferramenta de benchmark nova ligada às classes V2 reais + gastar crédito real de API agora, vs. deixar documentado) — escolheu **não construir a ferramenta nem gastar crédito agora**. Fica pendente, documentado, para quando o Felipe quiser rodar isso (com a ferramenta certa, a construir numa sessão futura, ou em staging/produção).
    - _Requisitos: 27.1, 27.2_
    - **Executado, REPROVADO (2026-09-23):** ferramenta nova `genesis:benchmark-brain-v2` (API, `app/Console/Commands/BenchmarkGenesisBrainV2.php` — mesmas peças do job, visão/contexto/bundle 1x, decisor Nx sem cache; relatórios em `storage/app/benchmarks/`). Gráfico real BTCUSDT.P 15m (22:21 UTC). Antes de medir, achou 2 bugs que derrubariam 100% das decisões com `decision_provider=gemini`: (1) enum INTEGER do `score` rejeitado pelo Gemini (HTTP 400 TYPE_STRING); (2) orçamento de complexidade do schema estourado pelas 3 listas de drivers (400 genérico, 4/4 no 3.6-flash, bissecção: estoura ao entrar `plan_b`). Correção: `forGemini()` sem enum não-STRING e sem `maxItems`; limites de lista passaram para `DecisionResponseValidator::arrayLimitErrors()` (`ARRAY_TOO_LONG:<campo>`). Resultado (13 decisões obtidas de 20 — 7 perdidas por 503 do Gemini + fallback OpenAI sem crédito): **7 SHORT / 6 LONG no MESMO bundle → DIRECTION_FLIP, bloqueia release (Fonte §92)**. Score estável (65×11, 70×2, amplitude 5). Regime RANGE 9/13. 10/13 decisões reprovariam no validador (LTA inexistente citada, números livres, elemento de target inventado). Gemini decisor sem `temperature` explícita (padrão do modelo), `thinkingLevel=HIGH`.
    - **Rodada 2 (2026-09-23), temperatura 0,2** (config nova opcional `GENESIS_GEMINI_DECISION_TEMPERATURE`, vazia = padrão do modelo; `--temperature` no comando): 11 decisões obtidas (9 perdidas por 503 + OpenAI sem crédito) — **10 SHORT / 1 LONG**, ainda FAIL. O único LONG é também a única decisão que cita LTA inexistente (seria rejeitada pelo validador → repair). As 5 decisões que passariam limpas no validador são todas SHORT. Score 65×9/70×2. Regime TREND 8/11 (antes RANGE 9/13).
    - **Rodada 3 (2026-09-23), temperatura 0 + `--delay=15 --retries=4`** (opções novas do comando; 503 do Gemini re-tenta a MESMA rodada com backoff): 19 decisões nas primeiras 19 rodadas — **16 SHORT / 3 LONG**, ainda FAIL. Um dos LONG (rodada 8) passaria limpo no validador — a troca não vem só da alucinação de LTA. LONG sempre com score 65; SHORT 65-75. Conclusão: temperatura reduz mas não elimina (thinking HIGH não é determinístico); o problema restante é a escolha de lado com vantagem marginal (score 65). Pendente decisão do Felipe: ajustar prompt, reduzir thinking para MEDIUM, ou rever a regra de aceite.
    - **Decisão do Felipe (2026-09-23): benchmark ACEITO como está** — não perseguir zero flip ("teve uma melhora bem boa já"). Critério §92 (zero flip) dispensado explicitamente por ele. `GENESIS_GEMINI_DECISION_TEMPERATURE` = 0 virou o padrão em `config/genesis_graphical_v6.php` (+ `.env.example`), testes em `GeminiDecisionClientTest`.
  - [x] 25.2 **[API]** Criar teste/feature-flag que desabilita `ScoreFromFamilies`/`score_breakdown` legado e roda o Brain V2 completo
    - Confirmar `direction`/`score`/Plano A/B/stop/targets/RR todos funcionando sem o mecanismo antigo
    - **Feito com um recorte real (2026-09-17):** não existe hoje nenhum ponto de chamada vivo de `ScoreFromFamilies` pra "desabilitar" — a task 5.1 (Fase 1.1) não colocou a chamada atrás de uma flag, REMOVEU ela inteiramente do job. Construir uma flag pra desligar um mecanismo já estruturalmente inalcançável seria inventar um controle morto. `LegacyScoreMechanismDisabledTest.php` novo transforma o grep manual da task 5.3 (feito uma vez, sem trava) numa prova PERMANENTE e automatizada: varre `app/` inteiro (recursivo, ~245 arquivos) procurando qualquer instanciação/resolução/injeção de `ScoreFromFamilies` fora da própria classe (falha se alguém reintroduzir no futuro) + confirma via Reflection que `ExecutionPipelineService` (o motor de Plano A/B/stop/targets/RR) nunca declara `ScoreFromFamilies` como dependência de construtor. "V2 roda completo sem o mecanismo antigo" não duplicado num teste novo — já provado por `GraphicalAnalysisFullPipelineIntegrationTest` (decisão real → persistência, direction/score) e `ExecutionPlanosArrayTest` (score → Plano A/B/stop/targets/RR completos, candles reais da Binance), ambos já verdes, cada um cobrindo a metade que testa melhor.
    - _Requisitos: 27.3_

- [ ] 26. Fase 8.2 — Auditoria final e Release Gate
  - [x]* 26.1 Rodar todos os comandos de auditoria da Fonte §94
    - `ScoreFromFamilies|score_familias|score_breakdown`; casts destrutivos de score; `Number(null)`-style no frontend; `execution.planoB|planoB`; `'3h'`; `MM_TIER1_FALLBACK|MATH_ESTIMATE_TIER1|0\.004|0\.005`
    - Confirmar que toda ocorrência remanescente é explicitamente legado/teste/histórico
    - **Feito (2026-09-17):** os 5 comandos literais da Fonte §94 rodados contra `app/`/`tests/`/frontend. `ScoreFromFamilies`/`score_familias`/`score_breakdown`: zero ocorrência viva (só comentários + testes isolados da própria classe + `tests/Proof/_archive_v4_3_r3_2` histórico) — travado permanentemente pela task 25.2. Casts destrutivos de score: zero (só o comentário que documenta a proibição). `'3h'`: backend só em tabelas de lookup mortas/normalizador de OCR (já confirmado inalcançável na task 4); **achado real no frontend** — `GenesisPage.tsx` ainda tem 2 arrays `validTimeframes` que ACEITAM `'3h'` de verdade (URL param e parsing de OCR), gap já registrado na própria task 3.8 ("precisa de teste visual real"), nunca fechado — usuário aceitaria `3h` no frontend só pra levar um 422 do backend. `MM_TIER1_FALLBACK`/`MATH_ESTIMATE_TIER1`: 100% ativos, mesmo conflito não resolvido da task 20.2/20.3. `execution.planoB|planoB`: **achado real** — `ExecucaoService.php:728` ainda envia `'planoB' => $planoB` (objeto completo, duplicando `execution.planos[1]`) na resposta pública; frontend confirmado limpo (task 16.3), backend nunca removeu a chave (decisão deliberada da própria task 16.3, documentada, de não tocar contrato de backend numa task só de frontend) — agora que estamos na auditoria final (Fase 8, que por doutrina §90.10 é o último momento pra limpeza de legado), fica marcado como pendência real, não fechada nesta task (ver relatório). Relatório completo publicado como artifact: https://claude.ai/artifact/TcMMnFiEBpBN6tujHRVyTe
    - _Requisitos: 27.4_
  - [ ] 26.2 Rodar suíte backend completa + suíte frontend completa + testes E2E críticos
    - **Feito (2026-09-17):** backend 1222 passed/5 falhas (as mesmas 2 categorias de flake pré-existente desta sessão inteira, reconfirmadas 0/5 em reruns isolados — não regressão). Frontend `vitest`: 896 passed/60 failed — as mesmas ~12 arquivos pré-existentes já documentados (duplicados pelo diretório órfão `.kilo/worktrees/steady-bactrosaurus/`), nenhuma falha nova; `tsc --noEmit` limpo. **Testes E2E críticos: não existem neste repositório** — nenhuma ferramenta de navegador (Playwright/Cypress/etc.) configurada; toda a cobertura desta sessão inteira é Feature test do Laravel (HTTP+fila+Binance reais) no backend e unitário/property-based no frontend, nunca um clique real em navegador (mesma ressalva já registrada honestamente no checkpoint da Fase 4, task 15).
    - _Requisitos: 28.1_
    - **Rodada 2026-09-23:** backend 1243 testes — 16 falhas, todas instabilidade conhecida (job/fila com Binance real, colisão de `users.email` em factory): os 5 arquivos afetados passaram 29/29 duas vezes seguidas isolados. `ManifestoV65Test` excluído da rodada: `exec("bash …")` resolve para o bash do WSL nesta máquina e trava (ambiente, não código). Frontend: `.kilo/worktrees/` (cópia antiga não versionada) excluído do vitest (commit `977558f`) — 58 falhas → 30; as 30 restantes são testes antigos de leitura de código-fonte (worker Python `SCORE_MINIMO=68`, menu "Radar Geopolítico", hook SSE, testes de exploração do Kiro) sem relação com o Brain V2 — cada um exige decidir se o código regrediu ou o teste envelheceu. E2E: ainda sem ferramenta.
    - **Frontend verde (2026-09-23, commits `337f840`/`9b574d2`):** 471 passaram, 30 pendentes com motivo no próprio teste, 0 falhas. Corrigidos bugs reais de cliente que os testes apontavam (ErrorBoundary na Carteira, anéis de progresso sem "0%" falso, cursor nos Planos A/B, rota morta `active_trades`). Pendentes por decisão de produto: paywall do scanner, endpoints admin `/admin/analises/zonas|tps`, remoção da cadeia morta `adaptedDataFetcher→interpretationEngine→indicatorEngine`. Obsoletos: fallback de modelo no frontend (proibido pela Fonte §85) e SSE do hook de alertas (trocado por polling).
  - [x] 26.3 Preencher o relatório de conclusão por item (`ITEM/STATUS/ARQUIVOS ALTERADOS/ARQUIVOS REMOVIDOS/SUBSTITUTO/TESTES EXECUTADOS/RESULTADO/RISCO RESIDUAL`)
    - Qualquer item incompleto precisa de aprovação explícita antes do deploy — padrão é não publicar parcialmente
    - **Feito (2026-09-17):** relatório completo (todas as 27 tasks, Fases 0-8) publicado como artifact — https://claude.ai/artifact/TcMMnFiEBpBN6tujHRVyTe — granularidade por task (não por sub-item individual, ~90 sub-itens seria denso demais pra um relatório de decisão; cada linha cobre status/resultado/risco residual). Veredito do próprio relatório: gate NÃO fechado, 2 itens bloqueados + 4 parciais de 21.
    - _Requisitos: 28.3_
  - [ ] 26.4 Confirmar os 21 itens do Release Gate (Fonte §90.11) aprovados
    - **NÃO confirmado — 15 de 21 aprovados, 4 parciais, 2 bloqueados (2026-09-17):** detalhe item a item no artifact acima. Bloqueados: **#17 Benchmark 20x** (task 25.1, Felipe optou por não rodar agora) e **#20 Testes E2E críticos** (ferramenta inexistente no repositório). Parciais: **#06 Slider/régua** (nunca testado em navegador real), **#09 Plano B vivo** (`execution.planoB` morto ainda enviado), **#12 Timeframes canônicos** (frontend ainda aceita `'3h'`), **#13 Brackets reais** (nunca confirmado contra Binance real + conflito MM_TIER1 sem decisão), **#16 Legado fora do decision path** (fragmentos residuais dos itens acima), **#19 Suíte frontend verde** (as ~12 falhas pré-existentes nunca foram zeradas, mesmo não sendo regressão desta sessão).
    - _Requisitos: 28.1_
  - [ ] 26.5 Confirmar que `GENESIS_BRAIN_V2_ENABLED` só é usado como rollback técnico emergencial, não como estratégia de rollout progressivo em produção
    - **Achado real, não confirmável como escrito (2026-09-17):** `rg -n "GENESIS_BRAIN_V2_ENABLED|brain_v2_enabled"` devolve ZERO ocorrências em código real (`app/`, `config/`, frontend) — a flag citada na Fonte (§89, `config/genesis_graphical_v6.php: 'brain_v2_enabled' => env('GENESIS_BRAIN_V2_ENABLED', false)`) nunca foi implementada nesta sessão; existe só dentro dos próprios documentos da spec. Esta sessão inteira implementou o V2 como substituição DIRETA e incondicional do código antigo (`ScoreFromFamilies` removido, não flag-gated; schema/prompt/config trocados sem branch condicional) — consistente com a doutrina da própria Fonte §89 ("código antigo não pode ficar ativo ao lado do V2 influenciando a mesma análise"), mas isso significa que hoje NÃO existe nenhum mecanismo de rollback técnico emergencial em runtime — a única forma de reverter seria reimplantar o deploy/commit anterior inteiro (nível de infraestrutura/ops, fora do que código resolve). Requer decisão do Felipe: construir a flag de verdade (contradiz parcialmente a doutrina de "sem código antigo paralelo"), ou aceitar que o rollback é só via redeploy.
    - _Requisitos: 28.2_

- [ ] 27. Publicação única — corte real de produção
  - Só depois de TODOS os checkpoints das Fases 0-8 aprovados e do Release Gate 100% fechado
  - **Não promover partes isoladas. Não deixar regra antiga ativa "temporariamente" dentro do caminho decisório novo** (Fonte §90.11)
  - **NÃO EXECUTADO, deliberadamente (2026-09-17):** a pré-condição da própria task ("Release Gate 100% fechado") não está satisfeita — task 26.4 confirmou 15/21 itens aprovados, não 21/21. A Fonte §90.11 é explícita ("não promover partes isoladas") e esta é, por design, uma ação de altíssimo risco/baixa reversibilidade (corte real de produção de um produto financeiro) — não é apropriado nem autorizado executá-la fora de um gate 100% fechado, com ou sem instrução de "executar 26 e 27" em lote. Os 2 bloqueadores reais (#17 benchmark, #20 E2E) e a ausência confirmada de `GENESIS_BRAIN_V2_ENABLED` (task 26.5) são decisões de produto que só o Felipe pode resolver — não algo que esta sessão deveria contornar ou decidir sozinha. Documentado no relatório (artifact acima) com uma recomendação: os itens de pequeno porte (fragmentos de legado residuais, #09/#12/#16) são seguros de fechar numa sessão futura se autorizado; os itens que dependem de ambiente real (Binance com credenciais, navegador real, orçamento de API) precisam do Felipe fora deste sandbox.
    - _Requisitos: 28.4_

## Notas

- Tarefas marcadas com `*` são testes; podem ser adiadas para um ciclo de desenvolvimento mais rápido, mas nenhum checkpoint de fase deve ser considerado concluído sem elas.
- `[API]` e `[FE]` indicam o repositório de cada tarefa; `[API/FE]` indica que a tarefa toca os dois lados.
- Tarefas de "investigação/confirmação" (2.2, 10.1, 12.1, 16.1, 20.1, 21.1-21.2) existem porque esta auditoria encontrou pontos onde o código real já pode estar mais avançado do que a Fonte presume — não pular direto para a implementação sem confirmar primeiro, sob risco de criar um mecanismo paralelo duplicado ao que já existe.
- A Fase 2 (features preditivas) e a Fase 0.3 (P0 independentes) podem rodar em paralelo à Fase 1 — nenhuma das duas depende do novo contrato de decisão para ser desenvolvida, só para ser conectada.
- Este spec não tem uma "seção opcional de saneamento de legado" como `genesis-cerebro-grafico-r3-2` teve — a limpeza de legado aqui é parte obrigatória da publicação única (Requisito 4, Fase 8), não um item futuro separado.
