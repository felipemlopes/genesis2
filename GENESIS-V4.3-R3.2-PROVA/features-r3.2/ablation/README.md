# Ablação — não realizada nesta entrega

Ver `../comparison-report.md`, seção "Gaps conhecidos", para o motivo: ablação real exige
histórico de trades/análises reais, que não existe ainda (sistema em shadow mode desde o
início desta implementação). Fica como pré-requisito explícito da Seção 21.4 do Documento
Mestre antes de desligar `GENESIS_SHADOW_MODE`.
